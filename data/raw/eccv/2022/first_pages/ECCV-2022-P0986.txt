1

Appendix A. Detailed Derivations and Proofs for Sec. 3.1
& 3.2
A.1 Details for Sec. 3.1
To better present our proposed vMF classifier, we formulate Eq. 2 in submission
PDF equivalently as:
  \begin {aligned} \label {vmf} p^l_i &= \frac {p^{tra}_{\mathcal {D}}(i) \cdot p(\tilde {\bm {x}}^l | \kappa _i, \tilde {\bm { \mu }}_i )}{\sum _{j=1}^C p_{\mathcal {D}}^{tra}(j) \cdot p(\tilde {\bm {x}}^l | \kappa _j, \tilde {\bm {\mu }}_j)} \\ &= \frac { \exp \{~ \kappa _i \cdot \tilde {\bm {x}}^l \tilde {\bm {\mu }}_i^{\top } + \overbrace {(\frac {d}{2}-1) \cdot \log \kappa _i - \log I_{\frac {d}{2}-1}(\kappa _i)}^{denoted~as~b_i} + \overbrace {\log n_i}^{prior} ~\} }{ \sum _{j=1}^{C} \exp \{~ \kappa _j \cdot \tilde {\bm {x}}^l \tilde {\bm {\mu }}_j^{\top } + \underbrace {(\frac {d}{2}-1) \cdot \log \kappa _j - \log I_{\frac {d}{2}-1}(\kappa _j)}_{denoted~as~b_j} + \underbrace {\log n_j}_{prior} ~\} }. \\ \end {aligned} 

(1)

Based on Eq. 1 in Appendix, we calculate the derivative of pli with respect to κi
as:
  \begin {aligned} \label {dki} \frac { \partial p_{i}^{l} }{ \partial \kappa _i } &= \frac { \partial p_{i}^{l} }{ \partial (\kappa _i \cdot \tilde {\bm {x}}^l \tilde {\bm {\mu }}_i^{\top } + b_i) } \cdot ( \frac { \partial (\kappa _i \cdot \tilde {\bm {x}}^l \tilde {\bm {\mu }}_i^{\top }) }{\partial \kappa _i} + \frac { \partial b_i }{\partial \kappa _i} ) \\ &= ( 1 - p_{i}^{l} ) \cdot ( \tilde {\bm {x}}^l \tilde {\bm {\mu }}_i^{\top } - A_d(\kappa _i) ), \end {aligned} 
(2)
where Ad (κi ) = Id/2 (κi )/Id/2−1 (κi ). The derivative of pli with respect to κj is
calculated as:
  \begin {aligned} \label {dkj} \frac { \partial p_{i}^{l} }{ \partial \kappa _j } &= \frac { \partial p_{i}^{l} }{ \partial (\kappa _j \cdot \tilde {\bm {x}}^l \tilde {\bm {\mu }}_j^{\top } + b_j) } \cdot ( \frac { \partial (\kappa _j \cdot \tilde {\bm {x}}^l \tilde {\bm {\mu }}_j^{\top }) }{\partial \kappa _j} + \frac { \partial b_j }{\partial \kappa _j} ) \\ &= - p_{j}^{l} \cdot ( \tilde {\bm {x}}^l \tilde {\bm {\mu }}_j^{\top } - A_d(\kappa _j) ). \end {aligned} 
(3)

The derivatives of pli with respect to µ̃i and µ̃j are formulated as:
  \begin {aligned} \label {dmu} \frac { \partial p_{i}^{l} }{ \partial \tilde {\bm {\mu }}_i } &= \frac { \partial p_{i}^{l} }{ \partial (\kappa _i \cdot \tilde {\bm {x}}^l \tilde {\bm {\mu }}_i^{\top }) } \cdot \frac { \partial (\kappa _i \cdot \tilde {\bm {x}}^l \tilde {\bm {\mu }}_i^{\top }) }{\partial \tilde {\bm {\mu }}_i} = (1-p_{i}^{l} ) \cdot \kappa _i \cdot \tilde {\bm {x}}^l \\ \frac { \partial p_{i}^{l} }{ \partial \tilde {\bm {\mu }}_j } &= \frac { \partial p_{i}^{l} }{ \partial (\kappa _j \cdot \tilde {\bm {x}}^l \tilde {\bm {\mu }}_j^{\top }) } \cdot \frac { \partial (\kappa _j \cdot \tilde {\bm {x}}^l \tilde {\bm {\mu }}_j^{\top }) }{\partial \tilde {\bm {\mu }}_j} = -p_{j}^{l} \cdot \kappa _j \cdot \tilde {\bm {x}}^l . \end {aligned} 
(4)

Implement Details. Both forward and backward operations with respect to bi
are not supported by Pytorch [3] framework. In addition, the floating point
precision for Iv (κ) with the large v and small κ (e.g., v = 511 and κ = 16) exceeds
f loat64 which is the maximum floating point precision of CUDA. While the
floating point precision for log Iv (κ) is in the normal interval.
To implement our method, we first calculate bi and its derivative by mpmath [1] library which allows the floating pointing operation with arbitrary
precision. Then, we convert them to the data type of Pytorch. Here is our core
code for the above steps:

