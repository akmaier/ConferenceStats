18

Lin, Hu, and Lu

6

Appendix

6.1

Validity of Eq. (14)

In the iterative process (13) that solves the problem (9a), it is formed by the
alternating iteration of two points: rt and xt . In Eq. (13a) (which is always called
the gradient descent step), the step size β is always chosen from (0, L1f ] [14][4][6],
2

where f (x) = 12 ∥y − Ax∥2 and Lf is the smallest Lipschitz constant of ∇f (x).
2
Indeed, Lf = ∥A∥2 , where ∥A∥2 is the spectral
 norm of A. From Eq. (13a), we
can observe that rt − xt−1 = βAT y − Axt−1 , which implies
  \left \|r^t-x^{t-1}\right \|_2^2&=\beta \left \|A^T\left (y-Ax^{t-1}\right )\right \|_2^2 \label {eq:rtxt-1}\\ &\leq \beta \left \|A^T\right \|_2^2\left \|y-Ax^{t-1}\right \|_2^2 \label {eq:rtxt-2}\\ &=\beta \left \|A\right \|_2^2\left \|y-Ax^{t-1}\right \|_2^2 \label {eq:rtxt-3}\\ &\leq \frac {1}{L_f}\left \|A\right \|_2^2\left \|y-Ax^{t-1}\right \|_2^2 \label {eq:rtxt-4}\\ &=\left \|y-Ax^{t-1}\right \|_2^2. \label {eq:rtxt-5}

(29e)
As the problem (9a) is convex provided Ψ is a linear operator, the PGD (proximal gradient descent) algorithm (which is the iterative process (13)) is globalconvergence [4][3]. Let x̄ = lim xt−1 , by inequality (29), we have
t→∞

  \lim \limits _{t\rightarrow \infty }\left \|r^t-x^{t-1}\right \|_2 \leq \left \|y-A\bar {x}\right \|_2. \label {eq:rx_eq} 

(30)

On the other hand, if the penalty coefficient λ of problem (9a) is small enough,
Pn
|xi |
the magnitude of the regularization term λ i=1 (|c̄ |+ε
is smaller than
)1−q
i

i

2

f (x) = 12 ∥y − Ax∥2 . This implies the optimal solution x̄ to the problem (9a)
2
leads to a relatively small value 12 ∥y − Ax̄∥2 . In other words, if λ is small enough,
by inequality (30), we can validate Eq. (14) as
  \bar {r}=\lim \limits _{t\rightarrow \infty }r^t\approx \lim \limits _{t\rightarrow \infty }x^{t-1}=\bar {x}. \label {eq:r_approx_to_x} 

(31)

Moreover, the two points rt and xt−1 are close if t is sufficiently large, β ∈
(0, L1f ], and λ is sufficiently small. Under the circumstance, instead of choosing
c̄ = xt as the solution to the problem (9b), the approximation
  \bar {c}\approx r^t, \label {eq:c-subproblem-2} 

(32)

where rt is the iterative point of the proximal gradient descent algorithm that
solves the problem (9a), is adopted. The benefits of using the approximate optimal solution instead of the exact optimal solution to problem (9b) will be
described in Sec. 3.3.

