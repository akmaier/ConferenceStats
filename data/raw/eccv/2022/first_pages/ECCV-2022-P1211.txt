Supplementary Material:
One-Trimap Video Matting
Hongje Seong1,⋆ , Seoung Wug Oh2 , Brian Price2 ,
Euntai Kim1 , and Joon-Young Lee2
2

A

1
Yonsei University, Seoul, Korea. {hjseong,etkim}@yonsei.ac.kr
Adobe Research, San Jose, CA, USA. {seoh,bprice,jolee}@adobe.com

Network Structure Details

In this section, we describe detailed network structures for trimap propagation,
alpha prediction, and alpha-trimap refinement.
Trimap propagation network. Fig. S1 shows a detailed architecture of the
trimap propagation network. The architecture is based on STM [11]. We employed
two independent ResNet50 [2] encoders to embed memory and query. Here, the
last layer (res5) is omitted to extract fine-scale features. The extracted memory
and query features are embedded into keys and values via four independent
3 × 3 convolutional layers. Using the memory key and query key, the similarity
is computed via non-local matching. Then the memory value is retrieved based
on the computed similarity. The retrieved memory value and query value are
concatenated along the channel dimension, and it is fed to the trimap decoder.
In the trimap decoder, several residual blocks [3] and upsampling blocks [12,10]
are employed. Finally, the propagated trimap is output from the trimap decoder.

1/8, 512

res3

1/8, 512

1/16, 1024

res4

1/16, 1024

𝑇𝑇𝑇𝑇𝑇𝑇 × 128

512 × 𝑇𝑇𝑇𝑇𝑇𝑇

matmul.

512 × 𝐻𝐻𝐻𝐻

matmul.
softmax

128 × 𝐻𝐻𝐻𝐻

𝑇𝑇𝑇𝑇𝑇𝑇 × 𝐻𝐻𝐻𝐻

𝐻𝐻𝐻𝐻 × 512

𝐻𝐻𝐻𝐻 × 512

Space-time Memory Read

concat.

𝐻𝐻𝐻𝐻 × 1024

3×3 conv

Trimap

Upsampling Block
ReLU

value (𝑄𝑄)

UpsamplingBlock

3×3 conv

key (𝑄𝑄)

1/4, 3

3×3 conv

key (𝑀𝑀)

UpsamplingBlock

3×3 conv

1/4, 256

3×3 conv

value (𝑀𝑀)

1/8, 256

𝐻𝐻 × 𝑊𝑊 × 1024

3×3 conv

𝑇𝑇 × 𝐻𝐻 × 𝑊𝑊 × 1024

3×3 conv, softmax

Trimap Encoder (𝑸𝑸)
Residual Block

concat.

Trimap Encoder (𝑴𝑴)

1/16, 256

res4

skip-connections

Residual Block

res3

add.

1/4, 256

add.

1/2, 64

res2

ReLU

conv1

1/4, 256

3×3 conv

1/2, 64

res2

Residual Block

conv1

×2 upsampling

skip-connection

1/16, 256

…

RGB

3×3 conv

RGB, Trimap, Alpha, Hidden

Trimap Decoder

Residual Block

Fig. S1. A detailed illustration of the trimap propagation network. Next to each block
in the figure, the relative spatial scale and channel dimension of the output are notated.
⋆

This work was done during an internship at Adobe Research.

