Secrets of Event-Based Optical Flow

A

Supplementary Material

A.1

Time-Awareness: PDE solutions

19

The proposed time-aware flow is given as the solution to (7). Letting the flow
be v = (vx , vy )> , the system of PDEs can be written as:
@vx
@vx
@vx
+ vy
+
= 0,
@x
@y
@t
@vy
@vy
@vy
vx
+ vy
+
= 0.
@x
@y
@t

vx

(10)

Upwind and Burgers’ schemes can be used to discretize and numerically solve
the system of PDEs [11, 41].
Discretization. Let vn (x, y) be the flow vector at discretized space- (e.g.,
pixel) and time-indices (x, y, n), with discretization steps x, y, and t, respectively, and let the forward (+) and backward ( ) di↵erences of a scalar field
w (e.g., vxn or vyn ) be defined as
@w +
1
=
w(x + x, y)
@x
x
@w +
1
Dy+ w ⌘
=
w(x, y + y)
@y
y

Dx+ w ⌘

w(x, y) ,
(11)
w(x, y) ,

and
@w
@x
@w
Dy w ⌘
@y

Dx w ⌘

1
w(x, y)
x
1
=
w(x, y)
y

=

w(x

x, y) ,

w(x, y

y) .

(12)

w(t))/ t, to
We discretize in time using forward di↵erences, @w
@t ⇡ (w(t + t)
@w
yield explicit update schemes: w(t + t) ⇡ w(t) + t @t .
Upwind scheme. The first-order upwind scheme is an explicit scheme that
updates the flow as follows, based on the sign of the variables: it uses Dx+ vxn
and Dx+ vyn for vxn > 0 (Dx vxn and Dx vyn otherwise), and Dy+ vxn and Dy+ vyn for
vyn > 0 (Dy vxn and Dy vyn otherwise). The scheme is stable if the flow satisfies
t max{|vx |/ x + |vy |/ y} < 1 (CFL stability condition [22]). For example, in
case that vxn > 0 and vyn > 0 at the current discretization time n:
vxn+1 = vxn

t vxn Dx+ vxn + vyn Dy+ vxn ,

vyn+1 = vyn

t vyn Dy+ vyn + vxn Dx+ vyn .

(13)

Burgers’ scheme. The study of the inviscid Burgers’ equation provides a
more conservative scheme solution, especially at “shock” and “fan wave” cases
[41]. In this explicit scheme, the product terms in the same variable (which

