Abbreviated paper title

8

Appendix

8.1

Proof of Theorem 1

17

Proof. Due to the symmetry of the data distribution in Eq. (5), if we want to
get the error rate of a linear classifier y = sign(x⊺ w), we only have to calculate
the integral of the Gaussian x ∼ N (µ∗ , Σ∗ ) over a half-space segmented by the
classifier. Suppose that the half-space that goes through the origin is given by
  \Omega _+=\{x\in \mathcal {R}^2|x^\intercal \boldsymbol {\mathbf {w}}\geq 0\}, 
(17)
based on the probability density of the Gaussian, the integral can be written as:
 \label {eq:integral} P=\frac {1}{2\pi \sqrt {\boldsymbol {\Sigma _*}}}\int _{\Omega _{+}}\exp \left [-\frac {1}{2}(x-\boldsymbol {\mu _*})^\intercal \boldsymbol {\Sigma _*^{-1}}(x-\boldsymbol {\mu _*})\right ]dx. 

(18)

To solve the integral (18), we need the coordinate transform as:
 \label {eq:cotrans} \Omega =\mathcal {R}^1\times [c,\infty ),\quad c=-\frac {\boldsymbol {\mu ^\intercal _*}\mathbf {w}}{\sqrt {\mathbf {w^\intercal }\boldsymbol {\Sigma _*}\mathbf {w}}}. 

(19)

With the transformation (19), the integral (18) becomes:
 \label {eq:simplify} P=\frac {1}{\sqrt {2\pi }}\int _c^\infty \exp \left (-\frac {1}{2}x^2\right )dx, 

(20)

where the integral over Ω can be evaluated by the error function.
Due to the monotony of the error equation, Eq. (20) can reach the optima
when c achieves its optima. According to the definition of c in Eq. (19), we have
that the minima of c can be achieved at w = Σ −1
∗ µ∗ , which exactly corresponds
to the the optima of objective (6). Thus, the Bayesian error rate of the classifier
can be derived as:
  err_*=1-\frac {1}{\sqrt {2\pi }}\int _c^\infty \exp \left (-\frac {1}{2}x^2\right )dx, 

(21)

µ⊺ w

where c = − √w⊺∗Σ w , w = Σ −1
∗ µ∗ , which completes the proof.
∗

8.2

Proof of Propositions 1 and 2

Propositions 1 and 2 are directly referred from [13]. Corresponding proof can be
found in [13].
8.3

Experiment Setting

Training: For both CIFAR10 and SVHN datasets, we used very similar training
schemes for different models. The images were first normalized to [0, 1]. ResNet
[11] and Wide ResNet [33] with different number of layers were employed as
the basic feature extractor. The standard perturbation size for the adversarial
8
training was both set to be ϵ = 255
. For all the implemented methods, the

