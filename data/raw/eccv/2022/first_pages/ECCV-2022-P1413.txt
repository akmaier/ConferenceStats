18

W. Shao et al.

Appendix
The appendix provides more details about the main paper in both methods in
Sec.A and experiments in Sec.B.

A

More Details about SFDA

A.1

Interpretation of weighted Kendall’s tau

The Kendall’s τ represents
  the ratio of concordant pairs minus discordant pairs
M
when enumerating all M2 pairs of {Tm }M
m=1 and {Gm }m=1 as given by
  \tau = \frac {2}{M(M-1)}\sum _{1\leq i < j \leq M}\mathrm {sgn}(G_i-G_j)\mathrm {sgn}(T_i-T_j) 

(10)

where sgn(x) is a sign function returning 1 if x > 0 and −1 otherwise. Moreover,
we use a weighted version of Kendall’s τ , denoted as τw , to evaluate transferability metrics considering that a best performing pre-trained model is always
preferred for target task in transfer learning. τw can measure the ranking performance of top performing models. In principle, a larger τw indicates the transferability metric can produce a better ranking for pre-trained models.
A.2

Algorithm of SFDA

Algorithm 1 Pipeline of SFDA.
M
1: Input: target dataset T = {(xn , yn )}N
n=1 ; M pre-trained models {ϕm }m=1 ;
2: Hyper-parameters: a = 4 in Eqn.(3) to generate the regularization coefficient.
3: Output: the transferability scores of SFDA, {Tm }M
m=1 ;.
4: Set: self-challenge = True
5: for m = 1 to M do
6:
calculate: static representations x̂ = θm (x).
7:
split: \protect \mathcal  {T}=\{\hat {x}_n^{(1)}\}_{n=1}^{N_1} \cup \cdots \cup \{\hat {x}_n^{(C)}\}_{n=1}^{N_C} .
\triangleright  total mean and class mean.
8:
calculate: \mu =\sum _{n=1}^N\hat {x}_n , \mu _c=\sum _{n=1}^{N_c}\hat {x}_n^{(c)} .
9:
calculate: S_W=\sum _{c=1}^{C}\sum _{n=1}^{N_c}(\hat {x}_n^{(c)}-\mu _c)(\hat {x}_n^{(c)}-\mu _c)\tran  .
\triangleright  within scatter.
10:
calculate:  S_B= \sum _{c=1}^CN_c(\mu _c - \mu )(\mu _c - \mu )\tran  .
\triangleright  between scatter.
\triangleright  regularization strength.
11:
calculate: \lambda =1 / (1+ \exp ^{-a\sigma (S_B)}).
12:
solve U : S_B U =[\mathrm {tr}(U\tran S_B U)/\mathrm {tr}(U\tran \tilde {S}_W U)] \tilde {S}_W U in Eqn.(4).
13:
calculate: updated representations x̃ = UT x̂ in Fisher Space.
14:
calculate: δc (x̂n ) = x̂nT UUT µc − 12 µcT UUT µc + log qc .
15:
calculate: p(y_n|x_n) = \exp ^{\delta _{y_n}(\hat {x}_n)}/{\sum _{c=1}^C\exp ^{\delta _{c}(\hat {x}_n)}}
16:
if self-challenge then
17:
calculate: \protect \hat  {x}_n = p_n \hat {x}_n + (1-p_n) \mu _{c\neq y_n}
\triangleright  ConfMix Noise.
18:
Set: self-challenge = False
19:
turn to line 7.
20:
end if
\triangleright  Transferability score of SFDA.
21:
Calculate: T_m = \frac {1}{N}\sum _{n=1}^N\log p(y_n|x_n).
22: end for

