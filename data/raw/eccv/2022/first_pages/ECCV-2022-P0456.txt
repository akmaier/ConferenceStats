Network Binarization via Contrastive Learning

6

Appendix

6.1

CMIM Algorithm

1

Algorithm 1 Forward and Backward Propagation of CMIM
Require: A minibatch of data samples
k
(X, Y), current binary weight WB
, lak
tent full-precision weights WF , and
learning rate η.
k0
Ensure: Update weights WF
.
1: Forward Propagation:
2: for k = 1 to K − 1 do
k
←
−
3:
Binarize latent weights: WB
k
);
sgn(WF
4:
Perform binary operation with the
−
activations of next layer: AkF ←
k
, Ak−1
XnorDotProduct(WB
B );
5:
Perform Batch Normalization:
− BatchNorm(AkF );
AkF ←
6:
Binarize full-precision activations
−
and obtain binary ones : AkB ←
sgn(AkF );

6.2

7: end for
n
o
8: For k = 1, · · · , K, pair ak,i
and
B
n
o
ak,j
as negative and positive pairs,
B
then use Eq. 13 layer by layer to compute the NCE loss LkN CE between AkB
and AkF for contrastive learning;

9: Combine a series of NCE loss LkN CE
with the classification loss L into the
CMIM loss LCM IM , with Eq. 14;
10: Backward Propagation: compute
the gradient of the overall loss func∂L
tion, i.e. ∂W
, using the STE to hanB
dle the sign function;
11: Parameter Update: update the fullk
i 0
−
←
− WF
precision weights: WF
∂L
η ∂Wk .
B

Experimental Results of CAD & WCoRD on BNNs

Utilizing additional full-precision networks as teachers, we experimentally implement those contrastive-based knowledge distillation methods (i.e. CRD [41]
and WCoRD [6]) into the binarization task, and we observe that CRD scales
poorly on BNNs. In particular, we use CRD and WCoRD in a standard way
(i.e. L = λ · LCRD + Lcls and L = λ · LW CoRD + Lcls ), and we fine-tune the models by adjusting the hyper-parameter λ in the loss function. Table 4 shows that a
naive extension preforms much worse than our proposed version (we only report
the best results w.r.t. λ). In fact, straight-forwardly introducing a FP network as
a teacher has already been comprehensively studied in Training Binary Neural
Networks with Real-to-Binary Convolutions (ICLR 2020). In this work, Martinez et al. points out that the existing KD methods can not be easily generalized
into BNN mainly because of the huge differences between full-precision and binary weights and activations. In practice, this inconsistency hinders the binary
and full-precision representations to be aligned within the knowledge distillation
framework. Note that more detailed comparisons between ours and CRD are
discussed in Section 3.3 (L408-432).

