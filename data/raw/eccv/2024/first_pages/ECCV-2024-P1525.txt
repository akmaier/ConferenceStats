Regulating Model Reliance on Non-Robust Features

1

Regulating Model Reliance on Non-Robust
Features by Smoothing Input Marginal Density
Supplementary Material
9

Proof

In this section, we provide proof of our proposed approach for estimating the
gradient diﬀerence using the gradients of the output diﬀerence.
Proof (Proof of Equation 7). Assuming that two functions, f and g, are continuously diﬀerentiable with respect to x 2 Rn , we can express them using their
Taylor series expansions, as

and

f (x) = f (a) + f 0 (a)(x

a) + o((x

a)2 ),

(10)

g(x) = g(a) + g 0 (a)(x

a) + o((x

a)2 ).

(11)

By subtracting Equation 11 from Equation 10, we have
f (x)

g(x) = f (a)

g(a) + (f 0 (a)

g 0 (a))(x

a) + o((x

a)2 ).

(12)

Next, we compute the gradients of both sides of Equation 12 with respect to x
as
rx (f (x) g(x)) = f 0 (a) g 0 (a) + o((x a)3 ).
(13)
Then, we can set x = a in Equation 13 as
ra (f (a)

g(a)) ⇡ f 0 (a)

g 0 (a).

(14)

Since we have assumed that f and g are diﬀerentiable, we can estimate the
diﬀerence between the gradients of the two functions as the gradient of the
diﬀerence between the functions as
rx (f (x)

g(x)) ⇡ rx f (x)

(15)

rx g(x).

Since the model f✓ parameterized with ✓ is assumed as continuously diﬀerentiable, we can substitute the model output logit fi (x; ✓) and log-softmax output
fi (x;✓)
logit log( eZf (x) ) into the functions f and g in Equation 15 as
rx fi (x; ✓)

rx log(

efi (x;✓)
) ⇡ rx (fi (x; ✓)
Zf (x)

log(

efi (x;✓)
)).
Zf (x)

(16)

Thus, the gradients of the diﬀerence between two outputs can be used to approximate the diﬀerence between the two gradients of the outputs.

