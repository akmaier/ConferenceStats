Gaussian Splatting on the Move

1

Supplementary Material
A

Method Details

A.1

Gaussian parametrization

In gsplat, the Gaussian covariances Σ in world coordinates are parametrized as
Σ = R(q) diag(sigmoid(s1 , s2 , s3 )) R(q)⊤

(11)

where q ∈ R4 is a normalized quaternion corresponding to a rotation matrix
R(q) ∈ SO(3) and (s1 , s2 , s3 ) ∈ R are scale parameters. This is not changed in
our implementation, but we use Σ instead of the above parameters in this paper
for brevity and clarity.
A.2

Transforming Gaussians from world to pixel coordinates

In 3DGS, the rendering equation can be written as,
(12)

Ci (x, y, P, G) = r(x, y, π(p̂(G, P )))

where p̂ : (µ, Σ, θ, P ) 7→ (µ̂, Σ̂, c) maps each (visible) Gaussian from the world
coordinate system to the camera coordinate system
µ̂i,j = Ri⊤ (µj − pi ),

Σ̂i,j = Ri⊤ Σj Ri ,

ci,j = θj (

µ j − pi
)
kµj − pi k

(13)

where the colour c is computed by evaluation the spherical harmonic function at
the normalized viewing direction. We use spherical harmonics of degree three.
The function π : (µ̂, Σ̂) 7→ (µ′ , d, Σ ′ ) projects Gaussians from camera coordinates
to pixel coordinates with depth:
d = µ̃z ,

µ′ = (µ̃x /d, µ̃y /d),

Σ ′ = Ji Σ̂Ji⊤ ,

(14)

where µ̃ = Ki µ̂ and Ji = J ′ Ki is the Jacobian matrix of the pinhole camera
projection µ̂ 7→ µ′ with intrisic camera matrix Ki :




fx 0 cx
1 1 0 −µ̃x /d
(15)
, Ki =  0 fy cy  .
J′ =
d 0 1 −µ̃y /d
0 0 1

Note that unlike the original gsplat [46] and the Inria implementations, we do
not use the OpenGL NDC coordinate system as an intermediate step between
projecting Gaussians to pixel coordinates.
The Gaussian with low depth d < dmin or pixel coordinates (µ′x , µ′y ) too far
outside the image boundaries [0, W ) × [0, H) are discarded in the next rendering
phases represented by the function r.

