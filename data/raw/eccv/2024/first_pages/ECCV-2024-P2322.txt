IFTR: An Instance-Level Fusion Transformer for
Visual Collaborative Perception
Shaohong Wang1 , Lu Bin1 , Xinyu Xiao2 , Zhiyu Xiang1 , Hangguan Shan1 , and
Eryun Liu1⋆
1
Zhejiang University, Hangzhou, China
{wangsh0111,lubin2022, xiangzy, hshan, eryunliu}@zju.edu.cn
2
Institute of Automation of Chinese Academy of Sciences, Beijing, China
xinyu.xiao@nlpr.ia.ac.cn

1

Loss Function

As mentioned in [1, 10], we use a set-to-set loss to measure the discrepancy
between the prediction set Ô = (B̂, ˆ
C) and the ground-truth set O = (B, C). This
loss consists of two parts: a smooth ℓ1 loss for regression and a focal loss [3] for
classification. Since the number of ground truth boxes M is usually smaller than
the number of predicted boxes NQ , we fill the set of ground truth boxes with ∅
(no object) up to NQ for computational convenience. To find a bipartite matching
between two sets, we employ the Hungarian algorithm to find a permutation of
the NQ elements σ ∈ P with the minimum cost. This is defined as:
σ ∗ = arg min
σ∈P

NQ
X

−1{ci ̸=∅} p̂σ(i) (ci ) + 1{ci ̸=∅} Lbox



bi , b̂σ(i)



i=1

where P denotes the set of permutations, p̂σ(i) (ci ) represents the probability of
class ci for the prediction with index σ(i), and Lbox represents the smooth ℓ1
loss for regression.
We then compute the total loss through classification loss and box loss as
follows:
L=

NQ
X

−λcls α(1 − p̂σ∗ (i) )γ log p̂σ∗ (i) (ci ) + 1{ci ̸=∅} λbox Lbox



bi , b̂σ∗ (i)



i=1

In the above equation, α, γ ∈ R are hyperparameters used to control the
balance between positive and negative samples, as well as the balance between
easy and difficult samples; λcls , λbox ∈ R are hyperparameters to weight the
different components of the loss function.
⋆

Corresponding author.

