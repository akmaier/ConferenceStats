Unsupervised Dense Prediction using
Differentiable Normalized Cuts
(Supplementary Material)
Yanbin Liu1

and Stephen Gould2

1

Auckland University of Technology
Australian National University
yanbin.liu@aut.ac.nz, stephen.gould@anu.edu.au
2

1

Technical Details

Training and Loss details. We first describe a symmetric version of the mask
consistency loss, denoted as L′mask . In this case, the features are obtained as:
f1′ = g(Φθ (I1 )), f2′ = q(g(Φθ (I2 ))), which swaps the features before Projector in
′
′
′
′
Fig. 2. And f1ovlp , f2ovlp = RoIAlign(f1′ , f2′ , t1 , t2 ). Then movlp
, movlp
can be
1
2
computed by NCut [5]. Finally,
  \label {eq:lossmask2} \mathcal {L}_{mask}^\prime &= \mathcal {L}_{bce}(\texttt {stop\_grad}(\bm {m}_1^{\text {ovlp}^\prime }),\,\bm {m}_2^{\text {ovlp}^\prime }).

(1)
′

′

We only compute losses for examples that have an IoU(movlp
, movlp
) > 0.5.
1
2
′

Given movlp
, we also enforce the structural regularization on it, similar to
2
Eqn. 11. Moreover, the symmetric version of the horizontal and vertical masks
(Fig. 3) is utilized for regularization.
For training, we adopt the Layer-wise Adaptive Rate Scaling (LARS) on top
of an SGD optimizer with a momentum of 0.9.
Structure of the Projector and Predictor. Both the Projector and Predictor are two-layer 2D ConvNets. The structure of the Projector is as follows Conv2D(384, 384) – BN – GELU – Conv2D(384, 256). The structure of
the Predictor is as follows Conv2D(256, 384) – BN – GELU – Conv2D(384, 256).
The number inside the parentheses indicates the input and output channels.
Prevent numerical instablity. One issue of differentiating the eigendecomposition problem is the numerical instability when two eigenvalues λi , λj get close
to each other3 . For example, the Pytorch function torch.linalg.eigh encoun1
. In this paper, we deal with the numerical issue
ters this issue. Let Kij = λi −λ
j
by setting Kij = 0 when |λi − λj | < 10−9 .
3

https://pytorch.org/docs/stable/generated/torch.linalg.eigh.html

