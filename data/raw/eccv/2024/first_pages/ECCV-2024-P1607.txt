Supplementary Materials for
“AttentionHand:
Text-driven Controllable Hand Image Generation
for 3D Hand Reconstruction in the Wild”

A

Preliminary: Latent Diffusion Model

Latent Diffusion Model (LDM) or Stable Diffusion (SD) [1] is a type of diffusion
model designed for training in a latent space, which is particularly well-suited
for likelihood-based generative models. Unlike traditional approaches that utilize
the full high-dimensional pixel space, LDM leverages the latent space to concentrate on the essential and meaningful aspects of the data. This enables training
in a lower-dimensional space, resulting in significantly improved computational
efficiency. The objective of LDM is as follows:
  L=\mathbb {E}_{z_0,t,c,\epsilon \sim \mathcal {N}(0,1)}[\|\epsilon -\epsilon _{\theta }(z_{t}, t, c)\|^2_2], 

(1)

where z0 is the initial latent image, zt is the noisy latent image after t diffusion
steps of z0 , c is the text embedding, and ϵθ is the diffusion training network.

B

Details of Data Preparation Phase

To train AttentionHand, it just requires easy-to-use four modalities: (1) a global
G
∈ R3×512×512 , which represents in-the-wild scene with
RGB hand image IRGB
G
hand, (2) the corresponding global hand mesh image Imesh
∈ R3×512×512 , (3) the
1×4
corresponding bounding box of hand region B ∈ R , and (4) the corresponding
hand-related text prompt U .
G
Rendering Hand Mesh Images. Imesh
is obtained by utilizing MANO [2],
which is generally adopted as ground-truth for 3D hand mesh reconstruction.
Specifically, the ground-truth root pose Mroot ∈ R1×3 , hand pose Mhand ∈
R15×3 , shape Mshape ∈ R1×10 , translation Mtrans ∈ R1×3 , and hand type h (i.e.,
left or right hand) are passed to MANO layer to get the mesh Mmesh ∈ R778×3
and face Mf ace ∈ R1538×3 of 3D hand as following:

  M_{mesh}, M_{face}=ManoLayer(M_{root},M_{hand},M_{shape},M_{trans},h). 

(2)

Then, by using Mmesh and Mf ace with ground-truth camera rotation matrix
R ∈ R3×3 , camera translation matrix t ∈ R1×3 , focal length f ∈ R1×2 , and
G
principal point p ∈ R1×2 , we can render the 2D mesh image Imesh
as following:
  I^{G}_{mesh}=Render(M_{mesh}, M_{face},R,t,f,p), 
where Render is operated based on PyTorch3D [3].

(3)

