TME for WILSS

6

1

Solution to Eq. (10)

We focus on the optimization problem
min L(✓, U, V)
U,V

s.t.

U  1, V  1, U + V

1.

(12)

Importantly, three constraints are imposed on U and V. The first two constraints
establish an upper limit on mask fusion, while the third constraint ensures that at
least one of the two masks actively contributes to the process. This optimization
problem can be rewritten as
X
⇥
⇤
min
Lb U R(S(xt , yt ), ) + V P c , (f✓t (xt ))c
U,V

s.t.

c2C t

U  1, V  1, U + V

1.

(13)

Note that the logistic function (·) is an element-wise operator. Therefore, the
optimization problem in Eq. (13) is element-wise independent. To simplify the
notation, we represent the elements of each symbol using lowercase letters.
Suppose r = R(S(xt , yt ), )ci and f = (f✓t (xt ))ci for 8c 2 C t , i = (h, w) 2 I,
with Lb being the binary cross-entropy loss, the optimization problem in Eq. (13)
can be transformed into a series of element-wise subproblems as
f
)(u · r + v · p)
1 f
u  1, v  1, u + v 1.

min L(u, v) = log(
u,v

s.t.

(14)

Given that the partial derivative of L(u, v) w.r.t. u or v is a constant, the change
of L(u, v) is monotonic when u or v increases or decreases, which implies that
the optimal solution of u and v must be achieved at the boundary. Therefore,
the solution to Eq. (13) can be easily derived as:
Uc =
Vc =

7

I(f✓t (xt )c > 0 _ R(S(xt , yt ), )c  Pc ) ,
I(f✓t (xt )c > 0 _ R(S(xt , yt ), )c > Pc ) .

(15)

Algorithm Procedure of Teddy

We present the complete procedure of Teddy in Alg. 1.

8

More Experiment Results

Here we present more complete experiment results including the disjoint setting
in Table 4. Besides, we provide ablation study on 10-5 VOC and COCO-to-VOC
settings in Tables 5-6.

