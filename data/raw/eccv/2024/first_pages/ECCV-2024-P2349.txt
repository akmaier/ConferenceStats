VividDreamer

A

Identify the problem in SDS

A.1

Implementation Details

19

We present here the specific details of our text-to-image generation experiments
to facilitate the reproduction of our experiments. We use threestudio [9] framework to implement our 2D experiments. For all results, we set the image resolution to 64 ⇥ 64 and use stable diﬀusion 2-1 base [41] as our base model. We set
the maximum iteration step number to 1000, the minimum time step and the
maximum time step to 20 and 980 respectively. We set the learning rate of the
target to 3e 2 (this is very important to reproduce our current results). We
keep other parameters consistent with the original parameters in threestudio [9].
A.2

Visualization of Over-smoothing

Fig. 10: We use a high-quality image as initialization and use diﬀerent t to add noise
(t=300 in the first column and t=600 in the second column). We use diﬀerent residual
terms to update the image. The results show the residual term derived from SDS leads
to serious over-smoothing.

In Sec. 4.1, we briefly discussed the issue of the reconstruction term ✏ (zt ; y, t)
✏. Here, we show more specifically the problems caused by the original reconstruction term during the generation process. According Eq. 5, we know that in
SDS, the next update of x can be seen as x ⌘w(t)(✏ (zt ; y, t) ✏), where ⌘ is the
learning rate. To better demonstrate the over-smoothing problem, we set ⌘ to 1
and show the results after the image update. As shown in Fig. 10, we find that
even if we use a high-quality image as the initial image, after updating the image
using SDS, the image becomes over-smoothing. Although in the actual process,
⌘ is a very small value (i.e., 1e 5), due to a large number of training steps,

