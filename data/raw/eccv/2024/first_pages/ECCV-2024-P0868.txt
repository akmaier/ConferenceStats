Supplemental Materials

OMR: Occlusion-Aware Memory-Based
Renement for Video Lane Detection
Dongkwon Jin1,2 and Chang-Su Kim1
1

School of Electrical Engineering, Korea University, Seoul, Korea
Samsung Advanced Institute of Technology
2

dongkwonjin@mcl.korea.ac.kr, changsukim@korea.ac.kr

A Other Datasets

Current Previous
frame
frame

Besides VIL-100 and OpenLane-V, there are some lane detection datasets, including TuSimple [1], LLAMAS [2], and CULane [3]. However, these datasets are
not suitable for video lane detection. TuSimple consists of multiple video clips,
each containing 20 consecutive frames. But, only the last frame in each video is
annotated, so the video metrics cannot be used. In LLAMAS, many annotation
errors were found, as in Fig. 1(a). Also, CULane exhibits low inter-frame correlation because the frame interval is too long, as in Fig. 1(b). Thus, it is regarded
as an image-level dataset.

(a) LLAMAS

(b) CULane

(a) There are many annotation errors in LLAMAS. (b) Two adjacent frames
do not exhibit high temporal correlation in CULane.
Fig. 1:

B Data Augmentation
We introduce a data augmentation scheme for training the OMR module. We
employ the KINS dataset. It is an amodal instance segmentation dataset, in
which a fully-shaped mask per object is given including its occluding parts.
Given an original video sequence, we randomly select an object from the KINS
dataset and then overlay its full shape onto the video frames. We also vary
the size and position of the object linearly over the frames. Fig. 2 shows some
examples of the synthetic data.

