FSGS: Real-Time Few-shot View Synthesis
using Gaussian Splatting
(Supplementary Material)
Zehao Zhu ⋆ , Zhiwen Fan⋆† , Yifan Jiang, Zhangyang Wang
†

Project Lead
The University of Texas at Austin

In the supplementary materials, details on experimental setups and implementations are provided in Appendix A. Additional quantitative and qualitative
results of FSGS are available in Appendix B.

A

More Technical Details

A.1

Initialization

Similar to 3D Gaussian Splatting [1], we start our pipeline from unstructured
multi-view images, and calibrate the images using Structure-from-Motion [4].
Next, we will continue the dense stereo matching under COLMAP with the
function “patch_match_stereo ” and utilize the fused stereo point cloud from
“stereo_fusion ”. We then initialize the SH coefficients at degree 0 and the positions of the 3D Gaussians based on the fused point cloud. Additionally, we
remain the rest coefficients and rotation to 0. We also initialize the opacity to
0.1 and set the scale to match the average distance between points.

A.2

Training

During training, we start with a SH degree of 0 for a basic lighting representation,
incrementing by 1 every 500 iterations up to a degree of 4 to increase complexity
over time. We set the learning rate of position, SH coefficients, opacity, scaling,
and rotation to 0.00016, 0.0025, 0.05, 0.005, and 0.001 respectively. At iterations
2000, 5000, and 7000, the opacity for all Gaussians is reset to 0.05 to eliminate
the low-opacity floaters. In the Blender dataset [3], the Pearson correlation is
only computed in pixels where the depth values are greater than 0. Additionally,
we utilize an open-source code1 to estimate the inverse depth map for both
the input images and the rendered images from pseudo views. We detail the
procedures of FSGS in Algorithm 1.
⋆
1

Equal Contribution.
https://pytorch.org/hub/intelisl_midas_v2/

