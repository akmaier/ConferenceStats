RePLAy

19

RePLAy: Remove Projective LiDAR Depthmap Artifacts
via Exploiting Epipolar Geometry
Supplementary Material
A

Proof of Lemma 1

Proof. Let vi ∈ V be a3D point from the set of 3D points(V) scanned by the
⊺
LiDAR and pi = u v 1 be its projection then,
  \begin {aligned} \pi (\mathbf {v}_i \mid \intrinsic ) = \intrinsic \rotMat \mathbf {v}_i + \intrinsic \transVec = d_i\leftPixel _i \end {aligned} 

(18)



where K is the intrinsic, R t is the extrinsic and d is the depth of the pixel
p. Since the virtual camera is situated at the LiDAR’s location, t = 0 in the
extrinsic matrix, and thus we get
  \begin {aligned} d_i\leftPixel _i = \intrinsic \rotMat \mathbf {v}_i \end {aligned} 

(19)

We know that K and R are full rank matrices and since di is scalar, we can
say that there is a one-to-one mapping between vi and pi in the virtual camera
setup. In other words, for the sensor pair of LiDAR and virtual camera we have:
  \begin {aligned} \forall \mathbf {v}_1, \mathbf {v}_2 \in \mathcal {V}, \;\Vert \pi (\mathbf {v}_1 \mid \intrinsic )-\pi (\mathbf {v}_2 \mid \intrinsic )\Vert _2 = \Vert \leftPixel _1 - \leftPixel _2\Vert _2 = 0 \\ \text {if} \; \Vert \mathbf {v}_1 - \mathbf {v_2}\Vert _2 = 0 \end {aligned} 

B

(20)

Proof of Lemma 3

⊺

⊺

Proof. Let p′ = p′x p′y 1 and q = qx qy 1 . Substituting this in Eq. (11) we
get
  \begin {aligned} t &= \|\leftPixel ' - \rightPixel \|_2 \\ &= \|\begin {bmatrix} \leftPixelSym _\xcoord ' & \leftPixelSym _\ycoord ' & 1 \end {bmatrix}^\transpose - \begin {bmatrix} \rightPixelSym _\xcoord & \rightPixelSym _\ycoord & 1 \end {bmatrix}^\transpose \|_2 \\ t &= \sqrt {(\leftPixelSym _\xcoord ' - \rightPixelSym _\xcoord )^2 + (\leftPixelSym _\ycoord ' - \rightPixelSym _\ycoord )^2} \end {aligned} \label {eqn:mag_eqn} 

(21)

Let us set the depth of p′ and q as d′ and d respectively, then:
  \small \begin {aligned} d \rightPixel &= d' \intrinsic _\stereoRight \rotMat \intrinsic _\stereoLeft ^{\text {-}1} \leftPixel ' + \intrinsic _\stereoRight \transVec \\ d \begin {bmatrix} \rightPixelSym _\xcoord & \rightPixelSym _\ycoord & 1 \end {bmatrix}^\transpose &= d' \intrinsic _\stereoRight \rotMat \intrinsic _\stereoLeft ^{\text {-}1}\begin {bmatrix} \leftPixelSym '_\xcoord & \leftPixelSym '_\ycoord & 1 \end {bmatrix}^\transpose + \intrinsic _\stereoRight \transVec \end {aligned} 

(22)

Applying pure translation conditions, i.e., R = I, we get:
  \rightPixelSym _\xcoord = \frac {d'\leftPixelSym _\xcoord ' + \xcoord }{d' + \zcoord }, \; \rightPixelSym _\ycoord = \frac {d'\leftPixelSym _\ycoord ' + \ycoord }{d' + \zcoord }

(23)

