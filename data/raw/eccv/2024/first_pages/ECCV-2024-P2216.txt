18

Yang et al.

A

Implementation Detail

A.1

Full List of Training Hyperparameters

Table 4: Hyperparameters for Inf-DiT Model
Training Hyperparameters of Inf-DiT
Number of Layers
28 Attention heads
16
Hidden size
1280 Attention head size
80
FFN inner hidden size 5120 Dropout
0
Warmup Steps
10k Peak Learning Rate 1e-4
Batch Size
320 Weight Decay
1e-4
Max Steps
1M Learning Rate Decay Linear
Gradient Clipping
0.1 Adam ✏
1e-8
Adam 1
0.9 Adam 2
0.099

A.2

Data Processing

Since the images generated by diﬀusion models often contain residual noise and
various detail inaccuracies, it becomes crucial to enhance the robustness of the
upsampling model to address these issues. We adopt the approach similar to
Real-ESRGAN [30] to perform a variety of degradation on low-resolution input
images within the training data.
When processing training images with a resolution higher than 512, there
are two alternative methods: directly performing a random crop, or resizing the
shorter side to 512 before performing a random crop. While the direct cropping
method preserves high-frequency features in high-resolution images, the resizethen-crop method avoids frequently cropping out areas with a single color background, which is detrimental to the model’s convergence. Therefore, in practice,
we randomly select from these two processing methods to crop training images.
A.3

Time and Space consumption

We have evaluated the time and space consumption with parallel size=16(generating
16*16 blocks at once) and compared it with other models in Table 5. Inf-DiT’s
consumption is similar to DemoFusion and ScaleCrafter at lower resolution
(2048, 4096), while achieving better quality. Note that Inf-DiT is implemented at
the pixel level while other diﬀusion models are implemented at the latent level,
therefore our input size is 64⇥ larger.

