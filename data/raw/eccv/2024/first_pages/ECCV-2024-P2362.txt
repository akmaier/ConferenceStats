Adaptive Bounding Box Uncertainties via Two-Step Conformal Prediction

A

Mathematical and algorithmic details

A.1

Split conformal prediction

1

The general conformal procedure for split conformal prediction [2, 43, 54, 65] is
provided in Algorithm 1 below.
Algorithm 1 Split conformal prediction
1: Input: data D ⇢ X ⇥ Y, prediction algorithm A, miscoverage rate ↵ 2 (0, 1).
2: Output: Prediction set/interval Ĉ(Xn+1 ) for test sample (Xn+1 , Yn+1 ).
3: Procedure:
4: Split data D into two disjoint subsets: a proper training set Dtrain and calibration
set Dcal = {(Xi , Yi )}n
i=1 .
5: Fit a prediction model on the proper training set:
fˆ(·)
A(Dtrain ).
6: Define a scoring function s : X ⇥ Y ! R applied to Dcal , resulting in a set of
nonconformity scores
n
S = {s(fˆ(Xi ), Yi )}n
i=1 = {si }i=1 .
A particular score si encodes a notion of dissimilarity (nonconformity) between the
predicted value fˆ(Xi ) and true value Yi .
7: Compute a conformal quantile q̂, defined as the
d(n + 1)(1 ↵)/ne-th empirical quantile of S.
Under exchangeability of Dcal [ {(Xn+1 , Yn+1 )}, the conformal quantile q̂ is a finite
sample-corrected quantile ensuring target coverage (1 ↵) by its construction.
8: For a new test sample (Xn+1 , Yn+1 ), a valid prediction set/interval for Xn+1 is
given by
Ĉ(Xn+1 ) = {y 2 Y : s(fˆ(Xn+1 ), y)  q̂}.
Validity refers to satisfying sample coverage with marginal probability (1 ↵), i.e.,
the guarantee in Eq. 1. We refer to the relevant literature for the proof.
9: End procedure

Rather than a marginal guarantee, we desire the class-conditional guarantee of
Eq. 2, which is re-interpreted as Eq. 4 for our object detection setting outlined
in § 2. Thus, we may relate our problem setting to Algorithm 1 as follows:
• A calibration sample (Xi , Yi ) consists of an input image Xi and an existing
object in that image, parametrized as (c1i , . . . , cm
i , li ) for an arbitrary amount
of coordinates m following § 2.3. As we run Algorithm 1 on a per-class
basis, the class label l for samples within a class is fixed, and we thus define
Yi := (c1i , . . . , cm
i ) as the object’s coordinates.
• Since Algorithm 1 is applied to samples within each class, the available
calibration set Dcal is partitioned into smaller sets of samples matching a
specific class label, i.e., we obtain sets
Dcal,y = {(Xi , Yi ) 2 Dcal : li = y}, 8y 2 Y.

