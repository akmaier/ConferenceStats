Inference Graphs for CNN Interpretation Supplementary Material
Yael Konforti† , Alon Shpigler† , Boaz Lerner, and Aharon Bar-Hillel
Ben-Gurion University of the Negev, Beer Sheva, Israel
{yaelkonf, alonshp}@post.bgu.ac.il; {boaz, barhille}@bgu.ac.il

Update Equations for MLP Modeling (Section 3.1)
In a batch EM formulation, model parameters are updated basedP
on sample
N
statistics of interest. Each statistic is defined as the sample average N1 i=1 f (Xi )
for a function f (X) of interest. In the online setting, for each such function f (X),
an online sample estimator is kept, denoted here by < f (X) >. Given a set of
examples {Xi }B
i=1 , < f (X) > is updated by
B

αX
< f (X) >t+1 = (1 − α) < f (X) >t +
f (Xi )
B i=1

(1)

where α is a smoothing factor. The tracked sufficient statistics are used in the
update of the model parameters as follows:
– The transition probability update tlk,k0 between hidden state k 0 in layer l − 1
and hidden state k in layer l is given by
< P (hl = k, hl−1 = k 0 |X, Θ) >
tlk,k0 = PK l
.
l
l−1 = k 0 |X, Θ) >
k=1 < P (h = k, h

(2)

The average joint distribution of clusters from consecutive layers < P (hl =
k, hl−1 = k 0 |X, Θ) > is a tracked statistic, computed for each example using the
forward-backward algorithm [2]. For the first layer, t1k is updated analogously
using t1k =< P (h1 = k|X, Θ) >.
– The mean µld,k for an estimated Gaussian before rectification (dropping the l
index for notation convenience), is given by
µd,k =

< P (h = k|x[d], Θ) · ŷ[d] >
< P (h = k|x[d], Θ) >

(3)

with ŷ defined by
ŷ[d] =
†

n

x[d],
x[d] > 0
M1 (µd,k , σd,k ), x[d] = 0

Equal contribution
Code for inference graphs algorithm released at github.com/yaelkon/GMM-CNN

