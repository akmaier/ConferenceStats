PointContrast: Unsupervised Pre-training for
3D Point Cloud Understanding
(Supplementary Material)
Saining Xie1 , Jiatao Gu1 , Demi Guo? , Charles R. Qi? ,
Leonidas Guibas2? , and Or Litany2?
1

Facebook AI Research
Stanford University

2

A

Visualization of the SR-UNet Architecture

Here we show the SR-UNet architecture that is used as a shared backbone in
our paper for both the pre-training and the fine-tuning phases. This U-Net architecture was originally proposed in [1] for ScanNet semantic segmentation.

Sparse Conv (1x1x1, D=class)

Sparse Deconv ResBlock (N=2, D=96)

Sparse Deconv ResBlock (N=2,D=96)

Sparse Deconv ResBlock (N=2,D=128)

Sparse Deconv ResBlock (N=2,D=128)

Sparse Conv Res Block (N=6,D=256)

Sparse Conv ResBlock (N=4,D=128)

Sparse Conv ResBlock (N=3,D=64)

Sparse Conv ResBlock (N=2,D=32)

Sparse Conv (3x3x3, D=32)

Sparse (De)Conv
ResBlock (N=2, D=32)

Sparse (De)Conv
(2x2x2, stride=2)
Sparse Conv
(3x3x3, stride=1)
Sparse Conv
(3x3x3, stride=1)
+
D: output dimension
N: number of repeated layers
Conv/DeConv layers are followed by BN and ReLU

Fig. 1: SR-UNet architecture we used as a shared backbone network for pre-training
and fine-tuning tasks. For segmentation and detection tasks, both the encoder and
decoder weights are fine-tuned; for classification downstream tasks, only the encoder
network is kept and fine-tuned.

?

Work done while at Facebook AI Research.

