Supplementary material:
Look here! A parametric learning based approach
to redirect visual attention
Youssef A. Mejjati1, Celso F. Gomez2, Kwang In Kim3,
Eli Shechtman2, and Zoya Bylinskii2
1

1

University of Bath, UK
2
Adobe Research
3
UNIST

Parameter definition

GazeShiftNet implements the following parametric transformations: sharpening, exposure, contrast, tone and color curve adjustment, as defined below.
Sharpness: Given an input image I and the predicted sharpness parameter
p1 ∈ [−2, 2], the output image is obtained by p
first computing image edges using
2
the Sobel filters f1 and f2 as follows: Iedge = (I ∗f
∗f2)2, where
∗ is the
1) +(I


−1 0 1
−1 −2 −1
convolution operation, and f1,f2 are the filters: f1 = 18 −2 0 2,f2 = 18  0 0 0 .
−1 0 1
1 2 1
Finally, the output image I 0 is calculated as I 0 =I +p1IedgeI.
Exposure: Given an input image I and the predicted exposure parameter p2 ∈[−3,3],
the output image is computed as I 0 =Iexp(p2 ∗log(2)), as in [5].
Contrast: Given an input image I and the predicted contrast parameter p3 ∈[−1,1],
the output image is obtained by the linear interpolation: I 0 =(1−p3)I +p3I 00, where
1
(1−cos(πIlum ))
I 00 =I 2
, and Ilum ==0.27R+0.67G+0.06B, as in [5].
Ilum
Tone and Color adjustment: We define color and tone adjustment using monotonic and piece-wise curve representations, in the same way as [5]. The curve is
represented using L different parameters, e.g. for tone adjustment pt ={pt0,pt1,...,ptL}.
PL−1
In this case, I 0 = PL1 pt i=0 clip(L.I −i,0,1)pti.
i=0 i

For tone adjustment, we define the same set of L parameters (∈[0,3]) for R, G, B.
While, for color adjustment three distinct sets of L parameters (∈ [0,3]) are defined
for R and G and B. We set L=8 for all our experiments.
In addition to the parameters defined above we experimented with additional parametric transformations, including white balance, blur, saturation, and gamma correction. For
white balance and gamma correction, we found that similar effects could be reproduced by
a combination of our other simpler parameters. We decided to focus on a smaller but sufficient subset, to avoid unnecessarily bulking up our model. Regarding saturation, we found

