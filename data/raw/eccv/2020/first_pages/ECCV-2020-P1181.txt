1

Supplementary Material for
OneGAN: Simultaneous Unsupervised Learning of
Conditional Image Generation, Foreground
Segmentation, and Fine-Grained Clustering
A

Summary of notation

For convenience, in Tab. 1 we provide a complete listing of the notation used in
our paper.

B

Regularization

Due to lack of space in the main text, we include the regularization loss terms
as part of the supplementary.
During generation, we apply regularization on the latent vectors and on the
mask image. The former serves to bound the range of the values to be close to
the axis center and to be closely grouped.
LRv = ||vp ||22 + ||vc ||22 + ||vbg ||22
The regularization on the mask serves to direct the model to utilize the
mask efficiently, with a balanced and decisive representation of background and
foreground. For mask Im ∈ [0, 1]H,W , with H, W the height and width of the
mask. The first regularization term balances the mask value around the value of
half.
HW
1
1 X
(
LMB = |
(Im )i,j ) − |
HW i,j
2
The second regularization term aims to make the masks more decisive. It is
0
= 2 · Im − 1.
better described when the mask is between [-1,1], so we define Im
In the ideal case, all pixels are either 1 or -1 (either background of foreground),
therefore, if we assume a balanced distribution, for each mask the average value
0
0
) is 0.5 and of min(0, Im
) it’s -0.5, since half of the pixels are zeroed
of max(0, Im
in each term. This is the decisiveness regularization.
LMD = |

HW
HW
1 X
1
1 X
1
0
0
(
max(0, (Im
)i,j )) − | + |
(
min(0, (Im
)i,j )) + |)
HW i,j
2
HW i,j
2

Together, the mask regularization loss is:
LRM = LMB + 0.1 · LMD

