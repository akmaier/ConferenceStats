This CVPR paper is the Open Access version, provided by the Computer Vision Foundation.
Except for this watermark, it is identical to the accepted version;
the final published version of the proceedings is available on IEEE Xplore.

BiLoRA: Almost-orthogonal Parameter Spaces for Continual Learning
Hao Zhu1, * Yifei Zhang2,∗ Junhao Dong2 Piotr Koniusz1,3,♢
1
Data61 CSIRO 2 Nanyang Technological University 3 Australian National University
allenhaozhu@gmail.com,

{yifei.zhang,junhao003}@ntu.edu.sg,

piotr.koniusz@data61.csiro.au

Abstract
Continual learning requires models to learn tasks sequentially while maintaining a delicate balance between stability (retaining knowledge of previous tasks) and plasticity
(adapting to new tasks). A key challenge is preventing interference between tasks which degrades performance when
learning new tasks over previously learned tasks. Recent
approaches leverage parameter-efficient fine-tuning (PEFT)
which adapts pre-trained models by injecting a small number of learnable parameters. However, existing PEFT-based
continual learning methods such as InfLoRA face fundamental limitations, i.e., they rely on complex optimization
procedures to learn orthogonal task-specific spaces which is
increasingly difficult as tasks accumulate. Thus, we propose
a novel bilinear reformulation that fundamentally reimagines the task separation through fixed orthogonal bases. Our
key insight is that by expanding the parameter space quadratically through two fixed bases, we can achieve “almost
orthogonal” task subspaces probabilistically, eliminating
the need for explicit interference elimination procedures.
We provide theoretical guarantees that this approach re
duces the probability
of task interference from O (k/d)2

to O (k/d2 )2 , ensuring reliable task separation without
complex optimization. Through extensive experiments on
ImageNet-R, CIFAR-100, and DomainNet, we validate our
theoretical bounds and demonstrate state-of-the-art performance with reduced parameter count. The code is available
at: https://github.com/yifeiacc/BiLoRA.

1. Introduction
Continual learning, where models must learn tasks sequentially while preserving knowledge of previous tasks, remains
a fundamental challenge in machine learning [26, 27, 32].
The key difficulty lies in maintaining a delicate balance between stability (retaining previous knowledge) and plasticity
(adapting to new tasks). Traditional approaches often suffer
* Equal contribution.

♢ Corresponding author.

from the so-called catastrophic forgetting [24, 29] where
learning new tasks significantly degrades performance on
previously learned tasks [1, 6, 7, 16, 19, 36, 40].
Recent advances in large-scale pre-trained models [4,
5, 11, 12] open new possibilities for addressing this challenge. However, adapting these foundational models efficiently while preventing interference between tasks remains
difficult. Parameter-efficient fine-tuning (PEFT) emerged
as a promising direction, particularly Low-Rank Adaptation (LoRA) [15] which introduces lightweight, learnable
components alongside frozen pre-trained weights.
The application of PEFT methods to continual learning
has shown initial success [38, 39], offering both parameter
efficiency and continuous adaptation capabilities. However,
current PEFT-based approaches face significant challenges
in balancing stability and plasticity [37]. The fundamental
issue lies in the shared parameter space where task-specific
updates become entangled, making it impossible to modify
one task’s parameters without affecting other tasks [39].
Recently, InfLoRA [22] attempted to address such an interference by enforcing orthogonality between task-specific
matrices. While this approach provides task separation guarantees, it faces two critical limitations: (1) The number of
tasks is strictly bounded by the dimensionality of the parameter space - with model dimension d and rank r, only
⌊d/r⌋ tasks can be supported. (2) Early tasks may claim
key parameter subspaces, leaving later tasks with potentially
suboptimal allocations that cannot be adjusted within a fixeddimensional parameter space [8, 34]. These limitations stem
from attempting to maintain perfect orthogonality within a
fixed-dimensional parameter space.
We present a fundamentally different approach to task
separation that leverages fixed orthogonal bases through a
novel bilinear reformulation [35]. Our key insight is that
while finding perfectly orthogonal subspaces becomes increasingly difficult with more tasks, we can achieve “almost orthogonal” spaces with high probability by expanding
the parameter space quadratically (as shown in Figure 1).
Specifically, by reformulating task adaptation through two
fixed orthogonal bases, we can theoretically guarantee that

25613

