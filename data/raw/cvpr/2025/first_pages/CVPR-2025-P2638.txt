This CVPR paper is the Open Access version, provided by the Computer Vision Foundation.
Except for this watermark, it is identical to the accepted version;
the final published version of the proceedings is available on IEEE Xplore.

VLMs-Guided Representation Distillation for Efficient Vision-Based
Reinforcement Learning
Haoran Xu1,2 Peixi Peng2,3∗ Guang Tan1∗ Yiqian Chang2,4 Luntong Li2 Yonghong Tian2,3,5
1
School of Intelligent Systems Engineering, Shenzhen Campus of Sun Yat-sen University
2
Peng Cheng Laboratory
3
School of Electronic and Computer Engineering, Shenzhen Graduate School, Peking University
4
School of Computer Science and Technology, Shenzhen Campus of Harbin Institute of Technology
5
School of Computer Science, Peking University
Feed Forward
Trained

Abstract
Vision-based Reinforcement Learning (VRL) attempts to establish associations between visual inputs and optimal actions through interactions with the environment. Given the
high-dimensional and complex nature of visual data, it becomes essential to learn a policy based on high-quality state
representation. To this end, existing VRL methods primarily rely on interaction-collected data, combined with selfsupervised auxiliary tasks. However, two key challenges
remain: limited data samples and a lack of task-relevant
semantic constraints. To tackle these challenges, we propose DGC, a method that Distills Guidance from Visual
Language Models (VLMs) alongside self-supervised learning into a Compact VRL agent. Notably, we leverage the
state representation capabilities of VLMs, rather than their
decision-making abilities. Within DGC, a novel promptingreasoning pipeline is designed to convert historical observations and actions into usable supervision signals, enabling semantic understanding within the compact visual
encoder. By leveraging these distilled semantic representations, the VRL agent achieves significant improvements
in sample efficiency. Extensive experiments on the Carla
benchmark demonstrate our state-of-the-art performance.

1. Introduction
Vision-based Reinforcement Learning (VRL) has emerged
as a powerful approach for learning complex control from
high dimensional observation (pixels) [15, 22, 23, 51]. It
has broad applications in visually rich domains, such as autonomous driving [24], robotics [62], and video game [25].
The success in these tasks heavily depends on the quality of
visual representations [14, 56], and therefore, recent work
has found it necessary to equip VRL with self-supervised
* Corresponding authors.

TD Loss
Fixed

Visual Obs.

Visual Obs.

visual
encoder

visual
encoder

Aux. Loss

Prompts
VLM

SSL
policy
decoder

policy
decoder

Ac�on

Ac�on

(a) SSL for VRL.

(b) VLM for Policy FT in VRL.

Visual Obs.

Visual Obs.

VLM
encoder

visual
encoder
SSL

Prompts
VLMs

SSL

policy
decoder

policy
decoder

Ac�on

Ac�on

(c) VLM for Representation FT in (d) VLMs-guided Representation
VRL.
Distillation in VRL (Ours).

Figure 1. Illustrations of different policy learning methods in
VRL: (a) SSL auxiliary tasks are employed to jointly learn state
representations alongside VRL [8, 14, 26, 55, 57, 59]. (b) VLM
outputs are used as prior knowledge for policy FT in VRL [29,
49, 50, 61]. (c) The pre-trained visual encoder of VLMs and SSL
tasks are combined for representation FT in VRL [9, 34, 37]. (d)
Our approach leverages pre-trained knowledge from VLMs along
with SSL auxiliary tasks to learn effective and robust visual representations, while policy optimization is handled by VRL.

learning (SSL) methods [8, 14, 26, 55, 57, 59], as shown
in Fig. 1a. These methods, combined with the Temporal
Difference (TD) loss [18, 19] inherent in VRL, jointly enable the learning of effective state representations, thereby
enhancing subsequent policy learning.

29534

