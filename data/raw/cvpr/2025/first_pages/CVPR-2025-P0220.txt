This CVPR paper is the Open Access version, provided by the Computer Vision Foundation.
Except for this watermark, it is identical to the accepted version;
the final published version of the proceedings is available on IEEE Xplore.

Mr. DETR: Instructive Multi-Route Training for Detection Transformers
Chang-Bin Zhang1
Yujie Zhong2
1
Visual AI Lab, The University of Hong Kong
jaszhong@hotmail.com

Abstract

+1.0

mAP

51

+1.2

+1.6

50

+1.9
+1.1

49

+3.1

+2.5

48

ep

)

TR

(24

-DE
Ali

gn

-DE

TR

gn

DIN
O

Ali

+
f+
De

f+

+

(90

(24

ep

0q

)

)

R
ET
De

f+

+

(30

0q

)

47

De

Existing methods enhance the training of detection transformers by incorporating an auxiliary one-to-many assignment. In this work, we treat the model as a multi-task framework, simultaneously performing one-to-one and one-tomany predictions. We investigate the roles of each component in the transformer decoder across these two training
targets, including self-attention, cross-attention, and feedforward network. Our empirical results demonstrate that any
independent component in the decoder can effectively learn
both targets simultaneously, even when other components
are shared. This finding leads us to propose a multi-route
training mechanism, featuring a primary route for one-toone prediction and two auxiliary training routes for oneto-many prediction. We enhance the training mechanism
with a novel instructive self-attention that dynamically and
flexibly guides object queries for one-to-many prediction.
The auxiliary routes are removed during inference, ensuring
no impact on model architecture or inference cost. We conduct extensive experiments on various baselines, achieving
consistent improvements as shown in Fig. 1. Project page:
https://visual-ai.github.io/mrdetr

kaihanx@hku.hk

baseline
boosted by Mr. DETR

52

H-D

cbzhang@connect.hku.hk

Kai Han1†
2
Meituan Inc.

Figure 1. Improvements over different baseline models. The
results are reported on the COCO 2017 validation in mean Average
Precision (mAP). From left to right: Deformable-DETR++ [69]
with 300 queries, H-DETR [21], Deformable-DETR++ [69] with
900 queries, Deformable-DETR++ [69] trained for 24 epochs,
DINO [60], Align-DETR [1] and Align-DETR [1] configured with
24 training epochs.
no object

no object

object
query
(a) one-to-one assignment

object
query
(b) one-to-many assignment

1. Introduction
The end-to-end detection transformer (DETR) [3], along
with subsequent studies [34, 41, 52], shine in object detection. Unlike traditional methods, DETR-based detectors eliminate the need for non-maximum-suppression
(NMS). They employ the one-to-one assignment strategy
(see Fig. 2(a)) for supervision, where each ground-truth box
is matched to a single prediction. Compared to the one-tomany assignment methods (see Fig. 2(b)) in conventional
object detectors [33, 44, 51] that allow each ground-truth
box to be assigned with multiple predictions, the one-to-one
assignment can lead to slow convergence [21, 49, 60] of
DETRs due to its sparse supervision.
To accelerate the training of DETR-like object detectors,

Figure 2. One-to-one vs. one-to-many assignments. (a) ‘One-toone’ assigns each ground-truth box to a single predicted box, while
predictions not associated with any object are supervised by ‘no
object’ (background). (b) ‘One-to-many’ allows each ground-truth
box to be paired with multiple predicted boxes.

several works propose auxiliary training methods to improve
the quality of prediction localization by introducing auxiliary one-to-many assignment [20, 21, 64] or multiple groups
of one-to-one assignment [5, 27, 60]. Specifically, DNDETR [27], Group-DETR, and DINO [60] utilize multiple
groups of parallel axillary queries that share the same transformer decoder with the primary object query. DETA [42]

† Corresponding author.

9933

