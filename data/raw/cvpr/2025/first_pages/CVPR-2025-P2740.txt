This CVPR paper is the Open Access version, provided by the Computer Vision Foundation.
Except for this watermark, it is identical to the accepted version;
the final published version of the proceedings is available on IEEE Xplore.

ImagineFSL: Self-Supervised Pretraining Matters on Imagined Base Set for
VLM-based Few-shot Learning
Haoyuan Yang 1,∗
1

Xiaoou Li 2,∗

Dalian University of Technology

Jiaming Lv 1
2

Xianjun Cheng 1

Qilong Wang 3

Beijing University of Posts and Telecommunications

3

Peihua Li 1 †

Tianjin University

https://peihuali.org/ImagineFSL

Abstract
Adapting CLIP models for few-shot recognition has recently attracted significant attention. Despite considerable
progress, these adaptations remain hindered by the pervasive challenge of data scarcity. Text-to-image models,
capable of generating abundant photorealistic labeled images, offer a promising solution. However, existing approaches simply treat synthetic images as complements to
real images, rather than as standalone knowledge repositories stemming from distinct foundation models. To overcome this limitation, we frame synthetic images as an imagined base set (iBase), i.e., an independent, large-scale
synthetic dataset encompassing diverse concepts. Building on this perspective, we introduce ImagineFSL, a novel
CLIP adaptation methodology that pretrains on iBase and
then fine-tunes for downstream few-shot tasks. We find
that, compared to no pretraining, both supervised and selfsupervised pretraining are beneficial, with the latter providing better performance. Based on on this finding, we
propose an improved self-supervised method tailored for
few-shot scenarios, enhancing the transferability of representations from synthetic to real image domains. Additionally, we present a systematic and scalable pipeline that employs chain-of-thought and in-context learning techniques,
harnessing foundation models to automatically generate diverse, realistic images. Validated across eleven datasets,
our methods consistently outperform state-of-the-art approaches by substantial margins.

tion [1]. FSL methods often rely on pretraining [2, 3] or
meta-training [4, 5] on a base set of classes with abundant
training examples, followed by meta-testing on a test set
with limited labeled data. Recently, large vision-language
models (VLMs) [6–8], particularly Contrastive LanguageImage Pre-training (CLIP) [8], have exhibited remarkable
few-shot and zero-shot recognition capabilities, drawing
significant attention [9, 10] in the FSL regime.
To adapt CLIP models for few-shot scenarios, various methods have been proposed, achieving substantial
progress [11–14]. Nevertheless, these methods continue
to struggle with the pervasive data-scarcity problem inherent in FSL. Although modern text-to-image (T2I) models [15, 16] can generate abundant photorealistic labeled
images, the full potential of synthetic data in FSL remains
underexplored. Existing approaches [17–19] simply treat
synthetic images as complements, directly fine-tuning CLIP
models using few-shot real images augmented with the synthetic ones during the meta-testing phase. At first glance,
this strategy appears reasonable, since CLIP models are already extensively pretrained on vast datasets, suggesting
that no further pretraining might be necessary.
However, we argue that such a strategy is sub-optimal
because it treats synthetic images merely as complements
to real images, instead of as standalone knowledge repositories derived from distinct foundation models. To overcome
this limitation, we frame synthetic images as an imagined
Base set (iBase), i.e., an independent, large-scale synthetic
dataset encompassing diverse concepts, on which adaptation methods can learn rich knowledge transferable to fewshot tasks. By attaching learnable adapters to frozen CLIP
encoders, we preserve the original representation capability
while granting adaptability to the newly introduced parameters. We present a novel methodology called ImagineFSL
(Figure 1), consisting of pretraining on iBase, followed by
fine-tuning for downstream FSL tasks with real images and
task-specific synthetic images. We find that, compared to
no pretraining, either self-supervised learning (Self-SL) or
supervised learning (SL) for pretraining is beneficial; crucially, Self-SL (e.g., DINO [20, 21]) outperforms SL.

1. Introduction
Few-shot learning (FSL), aiming to recognize novel concepts from limited labeled samples, has long been a subject of considerable research interest due to its potential
for reducing labor-intensive human annotation and cura∗ These authors contributed equally to this work. † Corresponding Author. The work was supported by National Natural Science Foundation of
China (62471083, 62276186, 61971086).

30020

