This CVPR paper is the Open Access version, provided by the Computer Vision Foundation.
Except for this watermark, it is identical to the accepted version;
the final published version of the proceedings is available on IEEE Xplore.

Cheb-GR: Rethinking k-nearest neighbor search in Re-ranking for Person
Re-identification
Jinxi Yang * He Li * Bo Du Mang Ye†
National Engineering Research Center for Multimedia Software,
School of Computer Science, Wuhan University, Wuhan, China
{jinxiyang, lihe404, dubo, yemang}@whu.edu.cn
https://github.com/Jinxi-Yang-WHU/Fast-GCR.git

Abstract
Person re-identification (ReID) is the task of matching individuals across different camera views. Existing approaches
typically employ neural networks to extract discriminative
features, ranking gallery images based on their similarities to probe images. While effective, these methods are
often enhanced through re-ranking, a post-processing step
that refines initial retrieval results without requiring additional model training. However, current re-ranking methods mostly rely on k-nearest neighbor search to extract
similar images that might have the same identity as the
query, which is time-consuming with a high computation
burden, limiting their applications in reality. We rethink
the effect of the k-nearest neighbor search and introduce
the Chebyshev’s Theorem-guided Graph Re-ranking (ChebGR) method, which adopts the adaptive neighbor search
guided by Chebyshev’s Theorem over the k-nearest neighbor search for efficient neighbor selection. Our method
leverages graph convolution operations to refine image features and achieve robust re-ranking, leading to enhanced
retrieval performance. Furthermore, we provide a theoretical analysis based on Chebyshev’s Inequality to elucidate the factors contributing to the strong performance of
the proposed method. Our method significantly reduces the
computation costs while maintaining relatively strong performance. Through extensive experiments in both general
and cross-domain settings, we demonstrate the effectiveness
of Cheb-GR and its potential for real-world applications.

involves matching individuals captured by different cameras based on their appearance, even when they are viewed
from different angles or under varying lighting conditions
[10]. Deep learning has become the predominant approach
in ReID, enabling automatic feature extraction and significantly enhancing performance [17, 21, 29, 30, 32, 33, 36,
41]. However, these methods primarily rely on pairwise
similarity comparisons between the query image and individual gallery images, neglecting the contextual relationships among images within the ranking list. Consequently,
they are susceptible to noise in feature representations arising from variations in illumination, pose, or occlusion.
To tackle this issue, many re-ranking methods have
emerged as a prominent strategy for enhancing performance
without the need for additional model training. Prevalent
approaches involve utilizing k-nearest neighbor search to
identify similar images, thereby extracting contextual information [25, 42]. However, this reliance on k-nearest neighbor search presents a significant bottleneck in real-world applications due to its computational complexity. Meanwhile,
many works utilize Graph Convolutional Networks (GCN)
to learn pairwise similarity information and relational information between images in person ReID tasks [1, 14, 27].
Notably, Zhang et al. [38] demonstrate the effectiveness of
GCNs for re-ranking by refining embeddings through feature propagation. Despite their potential, these GCN-based
methods still necessitate k-nearest neighbor search, which
is inherently sequential and hinders parallel computation,
leading to substantial time consumption.

1. Introduction

To improve re-rank efficiency, we propose the Chebyshev’s Theorem-guided Graph Re-ranking (Cheb-GR)
method that eliminates the need for the k-nearest neighbor
search and can be executed in parallel. Specifically, we construct a graph where each node represents an image. Unlike
GCR [38], which connects each node to its k-nearest neighbors, Cheb-GR uses feature distances as edge weights, discarding edges with extreme distances. The remaining dis-

Person re-identification (ReID) is a computer vision task
that aims to identify the same person across different camera views in a multi-camera surveillance system [5, 40]. It
* Equal contribution
† Corresponding Author: Mang Ye (yemang@whu.edu.cn)

19261

