This CVPR paper is the Open Access version, provided by the Computer Vision Foundation.
Except for this watermark, it is identical to the accepted version;
the final published version of the proceedings is available on IEEE Xplore.

Dynamic Pseudo Labeling via Gradient Cutting
for High-Low Entropy Exploration
Jae Hyeon Park1,†
1

Joo Hyeon Jeon1,†

Jae Yun Lee1,†

Sangyeon Ahn1

Min Hee Cha1

Min Geol Kim1,†
Hyeok Nam1
Sung In Cho1,† *
Department of Computer Science and Artificial Intelligence at Dongguk University
30, Pildong-ro 1-gil, Jung-gu, Seoul, 04620, Republic of Korea
{sy990607,rninicha,skagur10}@dgu.ac.kr
{pjh0011,jhbox7,stcaft,rlaalsrjf6,csi2267}† @dongguk.edu

Abstract
This study addresses the limitations of existing dynamic
pseudo-labeling (DPL) techniques, which often utilize static
or dynamic thresholds for confident sample selection. The
existing methods fail to capture the non-linear relationship between task accuracy and model confidence, particularly in the context of overconfidence. This can limit the
model’s learning opportunities for high entropy samples
that significantly influence a model’s generalization ability. To solve this, we propose a novel gradient pass-based
DPL technique that incorporates the high-entropy samples,
which are typically overlooked. Our approach introduces
two classifiers–low gradient pass (LGP) and high gradient
pass (HGP)–to derive over- and under-confident dynamic
thresholds that indicate the class-wise overconfidence acceleration, respectively. By combining the under- and overconfident states from the GP classifiers, we create a more
adaptive and accurate PL method. Our main contributions highlight the importance of considering both low and
high-confidence samples in enhancing the model’s robustness and generalization for improved PL performance.

1. Introduction
Such semi-supervised learning (SSL) [1, 4, 5, 10, 12, 14,
20, 27, 32, 40], domain adaptation (DA) [23, 28], and largevision pre-trained models [4, 12, 30] have actively applied
consistency training (CT) to improve a model’s task performance without an annoying annotation process. In CT,
anchor and positive views are generated by applying different data augmentations to a single data, and the difference or similarity between model’s outputs for the two
augmented inputs is defined as consistency loss. State-of* Corresponding author.

Gradient
||g||2

Anc.
Pos.

Important for generalization

Low Entropy
Large ||g||2

High Entropy
Large ||g||2

Overconﬁdent

Diverge d

Low Entropy
Small ||g||2

High Entropy
Small ||g||2

Converged

Underconﬁdent

: Anchor sample’s prediction
: Positive sample’s prediction

Anchor’s Entropy

Figure 1. Four stage suggestion that visualizes the model’s learning status based on the entropy and normalized gradients ||g||2 in
terms of the consistency training [17, 21, 33].

the-art pseudo-labeling (PL) techniques [18, 29, 32, 37, 38,
42, 46] minimize the classification loss for the labeled data
and the consistency loss for confident unlabeled samples.
“Confident” denotes samples in which the confidence of the
anchor view surpasses a certain threshold value. In PL,
thus, setting a threshold value is crucial to model training
where only confident predictions are used for backpropagation. Existing PL methods can be categorized into static
pseudo-labeling (SPL) approaches [18, 32], which employ
a fixed threshold value independent of the model’s learning status, and dynamic pseudo-labeling (DPL) approaches
[37, 39, 42], which adjust thresholds adaptively based on
the model’s learning status. SPL, which applies to a fixed
threshold, may restrict the learning opportunities of the

20602

