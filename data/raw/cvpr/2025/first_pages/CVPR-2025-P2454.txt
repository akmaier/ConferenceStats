This CVPR paper is the Open Access version, provided by the Computer Vision Foundation.
Except for this watermark, it is identical to the accepted version;
the final published version of the proceedings is available on IEEE Xplore.

STEPS: Sequential Probability Tensor Estimation for Text-to-Image Hard
Prompt Search
Yuning Qiu1 , Andong Wang1 , Chao Li1 , Haonan Huang1 , Guoxu Zhou2,∗ , Qibin Zhao1
1
RIKEN Center for Advanced Intelligence Project, Japan
2
Guangdong University of Technology, China
{yuning.qiu,andong.wang,chao.li,haonan.huang,qibin.zhao}@riken.jp, gx.zhou@gdut.edu.cn
https://github.com/ynqiu/STEPS

Abstract
Recent text-to-image (T2I) diffusion models have demonstrated remarkable capabilities in visual synthesis, yet their
performance heavily relies on the quality of input prompts.
However, optimizing discrete prompts remains challenging
because the discrete nature of tokens prevents the direct
application of the gradient descent method and the vast
search space of possible token combinations. As a result,
existing approaches either suffer from quantization errors
when employing continuous optimization techniques or become trapped in local optima due to coordinate-wise greedy
search. In this paper, we propose STEPS, a novel Sequential
probability Tensor Estimation approach for hard Prompt
Search. Our method reformulates discrete prompt optimization as a sequential probability tensor estimation problem,
leveraging the inherent low-rank characteristics to address
the curse of dimensionality. To further improve the computational efficiency, we develop a memory-bounded sampling
approach that shrinks the prompt space without the iteration step dependency while preserving sequential optimization dynamics. Extensive experiments on various public
datasets demonstrate that our method consistently outperforms existing approaches in T2I generation, cross-model
prompt transferability, and harmful prompt optimization,
validating the effectiveness of the proposed framework.

optimal text prompts has emerged as a critical challenge
[5, 19, 41, 42], as it directly determines the efficiency of
adapting large pre-trained models to specialized tasks.
The most straightforward approach is to learn the optimal
prompt through optimization methods. However, a significant challenge persists: the inherently discrete nature of
prompts prevents the effective application of traditional
continuous optimization algorithms, making it particularly
challenging to align text prompts with target objects.

1. Introduction

Current approaches have developed diverse strategies
to overcome this difficulty. One stream adopts continuous optimization strategies in embedding space followed
by discrete token projection [19, 41]. While this approach offers promising efficiency elegance, it inherently
introduces quantization errors during the projection phase.
The alternative paradigm applies the gradients with respect to the discrete one-hot token indicators and then
conducts a greedy search through coordinate-wise updates [35, 40, 42, 46]. Although this approach successfully
avoids quantization errors, the coordinate-wise optimization strategy has the issue of easily falling into bad local minima, limiting its ability of sufficiently exploring the
global prompt space. In addition to the aforementioned
methods, direct search methods have also been investigated
by employing random search [12] or greedy search and genetic algorithms [45] on a predefined vocabulary set. However, such approaches face limited exploration capability
due to the prohibitively huge search space (e.g., 4915210
candidates for a 10-token prompt with CLIP’s vocabulary
set).

Conditional text-to-image (T2I) generative models
[20, 29, 44] have revolutionized visual synthesis within
computer vision by demonstrating exceptional efficacy
in generating visual concepts. While these frameworks
demonstrate impressive generation abilities, their output
quality and consistency heavily depend on the precision of
input instructions. In this context, the ability to identify
∗ Corresponding Author.

To address these challenges, we propose a Sequential
probability Tensor Estimation approach for hard Prompt
Search (STEPS). Our approach reformulates discrete
prompt search as a sequential probability estimation problem, leveraging the low-rank characteristics of prompt joint
probability tensors and incorporating coarse-grained gradient information. Additionally, we introduce a memory-

28640

