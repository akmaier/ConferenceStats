This CVPR paper is the Open Access version, provided by the Computer Vision Foundation.
Except for this watermark, it is identical to the accepted version;
the final published version of the proceedings is available on IEEE Xplore.

Task-Specific Gradient Adaptation for Few-Shot One-Class Classification
Yunlong Li

Xiabi Liu* Liyuan Pan Yuchen Ren
Beijing Institute of Technology

{yunlong.li, liuxiabi, liyuan.pan, yuchen.ren}@bit.edu.cn

𝒙𝒙 𝑓𝑓𝑓𝑓𝜃𝜃 𝑔𝑔

Abstract
Optimization-based meta-learning methods for few-shot
one-class classification (FS-OCC) aim to fine-tune a metatrained model to classify the positive and negative samples
using only a few positive samples by adaptation. However, recent approaches primarily focus on adjusting existing meta-learning algorithms for FS-OCC, while overlooking issues stemming from the misalignment between
the cross-entropy loss and OCC tasks during adaptation.
This misalignment, combined with the limited availability
of one-class samples and the restricted diversity of taskspecific adaptation, can significantly exacerbate the adverse effects of gradient instability and generalization. To
address these challenges, we propose a novel Task-Specific
Gradient Adaptation (TSGA) for FS-OCC. Without extra
supervision, TSGA learns to generate appropriate, stable
gradients by leveraging label prediction and feature representation details of one-class samples and refines the adaptation process by recalibrating task-specific gradients and
regularization terms. We evaluate TSGA on three challenging datasets and a real-world CNC Milling Machine application and demonstrate consistent improvements over baseline methods. Furthermore, we illustrate the critical impact
of gradient instability and task-agnostic adaptation. Notably, TSGA achieves state-of-the-art results by effectively
addressing these issues.

1. Introduction
One-class classification (OCC) is an essential task in various fields, e.g., novelty detection and anomaly detection in
cybersecurity and medical diagnosis applications [9, 37, 39,
54]. The objective of OCC tasks is to classify new samples
as either positive or negative when only positive samples
are available during training. While previous OCC methods [1, 7, 16, 17, 31, 32, 36, 40, 41, 45, 46, 48] often require
extensive amounts of positive data to train a classifier, fewshot settings frequently involve scenarios where only lim* Corresponding author.

𝒙𝒙 One-class samples
𝑦𝑦 Labels (positive)

∇𝜽𝜽 ℓ

(a) OC-MAML

𝑦𝑦� 𝑦𝑦 𝒙𝒙 𝑓𝑓𝑓𝑓𝜃𝜃 𝑔𝑔

Forward
Backward

𝜷𝜷𝜽𝜽

𝜽𝜽

𝒓𝒓, 𝑦𝑦�

𝜶𝜶∇𝜽𝜽 ℒ 𝒜𝒜 ∇𝜽𝜽 ℒ 𝒩𝒩
(b) TSGA

Figure 1. An overview of OC-MAML and the proposed TSGA.
Both (a) and (b) allow rapid adaptation of the model parameterized by θ, comprising a feature extractor f and a decision maker
g. Here, x and y denote the input data and corresponding label
(only positive), respectively. Different from OC-MAML which
uses the cross-entropy loss ℓ in adaptation, we employ Loss Network N to generate appropriate losses L by leveraging the representations r = f (x) and predictions ŷ = g(r). Then, we use
Adapter Network A to obtain task-specific gradients α∇θ L and
regularization terms βθ, which refines the adaptation process.

ited positive examples (i.e., “shots”) are available, as seen
in many real-world applications, e.g., only small datasets
are available in industrial manufacturing [52]. Humans intuitively perform the few-shot OCC (FS-OCC) tasks, yet
machines still struggle to accomplish them effectively.
Mainstream approaches to address the FS-OCC rely on
meta-learning [20], which generally fall into two categories:
1) metric-based [12, 25, 42] and 2) optimization-based [15]
methods. The former seeks to learn discriminative representations that separate positive from negative query samples, while the latter fine-tunes a meta-trained classifier by
adaptation (i.e., a few gradient steps) on one-class support
samples. Due to more flexible adaptation and better task
generalization, optimization-based meta-learning methods
demonstrate significant potential for FS-OCC, e.g., OneClass Model-Agnostic Meta-Learning (OC-MAML [15])
is developed based on the popular meta-learning approach
MAML[14] by simply adapting the episode generation
strategy and aims to find a model initialization that can classify novel query samples after adaptation on the one-class
support set (as shown in Figure 1a).
Following this paradigm, optimization-based approaches
primarily focus on adapting existing algorithms (e.g., MetaSGD [28] and MetaOptNet [27]) to FS-OCC tasks, while

30556

