FSRNet: End-to-End Learning Face Super-Resolution with Facial Priors
Yu Chen1,5∗ Ying Tai2∗ Xiaoming Liu3 Chunhua Shen4 Jian Yang1
1
2
Nanjing University of Science and Technology
Youtu Lab, Tencent
3
4
5
Michigan State University
University of Adelaide
Motovis Inc.
https://github.com/tyshiwo/FSRNet

Target

Input (Bicubic)

VDSR

URDGN

SRResNet

FSRNet (Ours) FSRGAN (Ours)

Figure 1: Visual results of different super-resolution methods.

Abstract

1. Introduction

Face Super-Resolution (SR), a.k.a. face hallucination,
Face Super-Resolution (SR) is a domain-specific superaims to generate a High-Resolution (HR) face image from a
resolution problem. The facial prior knowledge can be
Low-Resolution (LR) input. It is a fundamental problem
leveraged to better super-resolve face images. We present
in face analysis, which can greatly facilitate face-related
a PSNR/SSIM
novel deep end-to-end
trainable 15.5/0.58/1.38
Face Super-Resolution
15.9/0.58/1.18
14.9/0.55/1.24
tasks, 17.0/0.60/0.95
e.g., face alignment [16, 25, 36], face parsing [23],
Network (FSRNet), which makes use of the geometry prior,
face recognition [34, 41], and 3D face reconstruction [29],
i.e., facial landmark heatmaps and parsing maps, to supersince most existing techniques would degrade substantially
resolve very low-resolution (LR) face images without wellwhen given very LR face images.
aligned requirement. Specifically, we first construct a
As a special case of general image SR, there exists facecoarse SR network to recover a coarse high-resolution (HR)
specific prior knowledge in face images, which can be
image. Then, the coarse HR image is sent to two branches:
pivotal for face SR and is unavailable for general image
a fine SR encoder and a prior information estimation netSR [22, 32, 33]. For example, facial correspondence field
work, which extracts the image
features,
and estimates
land- Target
CoarseSR
SRResNet
FineSR
Bicubic
could
help recover accurate face shape [46], and facial commark heatmaps/parsing maps respectively. Both image feaponents
reveal rich facial details [31,40]. However, as comtures and prior information are sent to a fine SR decoder to
pared
in
Tab. 1, the previous face SR methods that utilize
recover the HR image. To generate realistic faces, we also
facial
priors
all adopt multi-stage, rather than end-to-end,
propose the Face Super-Resolution Generative Adversarial
training
strategies,
which is inconvenient and complicated.
Network (FSRGAN) to incorporate
adversarial
loss into PSNR/SSIM
26.58/0.7659
24.16/0.6670the
25.33/0.7196
23.47/0.6267

FSRNet. Further, we introduce two related tasks, face alignment and parsing, as the new evaluation metrics for face SR,
which address the inconsistency of classic metrics w.r.t. visual perception. Extensive experiments show that FSRNet
and FSRGAN significantly outperforms state of the arts for
very LR face SR, both quantitatively and qualitatively.
Bicubic/CoarseSR

SRResNet/FineSR

∗ indicates equal contributions. This work was partially done when Yu
Chen was visiting University of Adelaide. J. Yang is the corresponding
author.

Based on deep Convolutional Neural Network (CNN),
in this work, we propose a novel end-to-end trainable Face
Super-Resolution Network (FSRNet), which estimates facial landmark heatmaps and parsing maps during training,
and then uses these prior information to better super-resolve
very LR face images. It is a consensus that end-to-end training is desirable for CNN [16], which has been validated in
many areas, e.g., speech recognition [8] and image recognition [20]. Unlike previous Face SR methods that estimate
local solutions in separate stages, our end-to-end framework
learns the global solution directly, which is more convenient
and elegant. To be specific, since it is non-trivial to estimate
12492

