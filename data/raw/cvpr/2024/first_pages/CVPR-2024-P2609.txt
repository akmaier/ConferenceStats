This CVPR paper is the Open Access version, provided by the Computer Vision Foundation.
Except for this watermark, it is identical to the accepted version;
the final published version of the proceedings is available on IEEE Xplore.

LDP: Language-driven Dual-Pixel Image Defocus Deblurring Network
Liyuan Pan1 † , Yan Yang2 ,
1
Beijing Institute of Technology

Hao Yang1 ,

{hao.yang, liyuan.pan}@bit.edu.cn,

Richard Hartley2 , and Miaomiao Liu2
2
Australian National University

{yan.yang, richard.hartley, miaomiao.liu}@anu.edu.au

Ours

Abstract

27.0

K3DN

(CVPR’23)

Ours

Recovering sharp images from dual-pixel (DP) pairs
with disparity-dependent blur is a challenging task. Existing blur map-based deblurring methods have demonstrated
promising results. In this paper, we propose, to the best
of our knowledge, the first framework that introduces the
contrastive language-image pre-training framework (CLIP)
to accurately estimate the blur map from a DP pair unsupervisedly. To achieve this, we first carefully design text
prompts to enable CLIP to understand blur-related geometric prior knowledge from the DP pair. Then, we propose a format to input a stereo DP pair to CLIP without
any fine-tuning, despite the fact that CLIP is pre-trained on
monocular images. Given the estimated blur map, we introduce a blur-prior attention block, a blur-weighting loss, and
a blur-aware loss to recover the all-in-focus image. Our
method achieves state-of-the-art performance in extensive
experiments (see Fig. 1).

1. Introduction
Defocus deblurring from a single image is an ill-posed
problem [19, 28, 29, 33]. Compared to methods that restore an all-in-focus image directly [1, 2], blur map-based
deblurring methods have demonstrated promising results
[16, 26]. To simplify the blur removal process, several
works [20, 34, 38] have utilized recent innovative dual-pixel
(DP) sensors [6] to estimate blur maps, since the DP sensor captures a two-view image pair (a DP pair) in a single
shot. The DP pair can be treated as images from a twosample light field camera or a stereo system [24] with a tiny
baseline [27, 34], which is beneficial for disparity estimation [1, 14, 23, 42]. Based on the DP image pair formation process, the disparity is geometrically related to the
blur map, i.e., the degree of defocus blur proportional to
disparity magnitudes [9].
For estimating the blur map (defocus map or disparity
† Corresponding author.

PSNR (dB)

Restormer
(CVPR’22)

BAMBNet
(CAA’21)

26.5
IFAN

(CVPR’21)

DeepRFT

26.0

25.5

(AAAI’22)

RDPD

DDDNet

(ICCV’21)

(CVPR’21)

0.05

0.1
0.15
Inference time (S)

0.20

0.25 0.30

Figure 1. Comparison of performance in terms of PSNR and Inference time. Our small and large models are respectively denoted
by the Blue and Red cycles. The Green triangles represent previous state-of-the-art methods, and each of them is annotated with
the method name, publication venue, and year. This figure is best
viewed in color on the screen.

map), previous works either require extra data [3, 26], such
as synthetic data, for supervision signals, or pre-calibrated
blur kernels [34]. To avoid using extra information, several
works explored unsupervised blur map estimation frameworks [16, 20]. Lee et al. [16] implicitly estimate the disparity of the left/right views using a spatial transformer, yet
only in feature space, as well as Yang et al. [38]. Liang et
al. [20] use fixed network branches for layer-wise defocus
map estimation and deblurring, where the limited network
branch number restricts the potential of its accuracy.
Recently, the contrastive language-image pre-training
framework (CLIP) [30] has accomplished extraordinary
success for vision tasks, such as semantic segmentation [31,
37], object detection [32], and point cloud understanding [40]. Therefore, a question naturally arises, can we
avoid the cost of collecting data and model designing, to
estimate the blur map unsupervisedly by using CLIP? Unfortunately, using semantic knowledge from CLIP to handle
low-level vision tasks is underexplored.
In this paper, for the first time, we explore the relationship between language and blur map estimation by applying CLIP. Note that CLIP is trained to match a monocular
image with its corresponding language descriptions. Given
a blurred image, it is intuitive to match it with a corre-

24078

