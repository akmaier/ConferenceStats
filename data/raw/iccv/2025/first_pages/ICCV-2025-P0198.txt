This ICCV paper is the Open Access version, provided by the Computer Vision Foundation.
Except for this watermark, it is identical to the accepted version;
the final published version of the proceedings is available on IEEE Xplore.

SemiVisBooster: Boosting Semi-Supervised Learning for Fine-Grained
Classification through Pseudo-Label Semantic Guidance
Wenjin Zhang1,2

Xinyu Li2 Chenyang Gao1,2 Ivan Marsic1
Rutgers University 2 Amazon
{wz315, cg694}@scarletmail.rutgers.edu, lixinyu.arthur@outlook.com, marsic@rutgers.edu
1

Abstract
Deep learning models rely on large-scale labeled datasets,
but collecting such data is expensive and time-consuming.
Semi-supervised learning (SSL) mitigates this issue by
learning from a small set of labeled samples along with a
large pool of unlabeled data. However, existing SSL methods struggle with fine-grained classification when dealing
with visually similar classes, as they rely solely on visual
features and ignore the semantic information within label
names. This paper introduces SemiVisBooster, an SSL
enhancement approach that utilizes semantic information
from label names to guide visual feature learning, addressing the challenges of fine-grained classification. By aligning text embeddings from label names with visual features,
our method helps the model capture subtle visual distinctions that purely visual representations may overlook. To
enhance robustness, we propose two key components: (1)
text embedding de-similarity (TEDS) to reduce confusion
caused by similar text embeddings across different class
names, and (2) class-aware visual-text alignment loss to
accurately define positive and negative pairs during visualtext alignment. Our method achieves state-of-the-art performance on the latest SSL benchmarks. Additionally, on
the challenging Food-101 dataset, which contains many visually similar classes and uses only 404 labeled images, our
approach improves performance by approximately 13.6%
over the second-best method. Code is available at SemiVisBooster

Figure 1. Comparison of visual feature space distribution for (a)
FreeMatch and (b) our method on the Food-101 dataset visualized
using t-SNE. We only plot the first 20 classes.

large amount of unlabeled data [5, 21, 36, 41, 46]. By
significantly reducing the dependence on manual annotations, SSL has demonstrated great potential in practical applications. Despite achieving strong performance in general image classification, particularly on datasets with wellseparated class boundaries such as CIFAR-10 [19] and
STL-10 [6], SSL methods struggle with fine-grained classification, where visually similar categories exist. Analyzing the confusion matrix of the popular SSL method,
FreeMatch [41], on the CIFAR-100 dataset with only
200 labeled images [42], we observed that its accuracy plateaued at 78.9% due to frequent misclassifications
among visually similar classes such as “woman,” “man,”
“boy,” and “girl” [40]. This observation indicates a key
limitation of existing SSL approaches: their reliance on
visual feature extraction, which often fails to distinguish
subtle inter-class differences. To further validate the observed limitation on SSL, we evaluated FreeMatch on the
fine-grained Food101 dataset [3] using 404 labeled images because this dataset contains many visually similar
classes. FreeMatch achieved an accuracy of only 60.4%,
while a fully supervised training method using all available
labeled data reached 98.8% [1], highlighting a significant
performance gap between SSL and fully supervised learning. This result underscores the need for improvements in
SSL methods. The t-SNE visualization (Figure 1) illustrates that while FreeMatch exhibits some level of clustering, it struggles to separate visually similar classes, leading to significant feature overlap. This challenge arises
from two key weaknesses in existing SSL methods. (1).

1. Introduction
The superior performance of deep learning heavily relies
on supervised training with sufficient labeled data [14, 39].
However, acquiring such massive labeled datasets for realworld applications remains a labor-intensive and expensive process. To mitigate this challenge, semi-supervised
learning (SSL) has emerged as a promising method, learning from a small set of labeled data while leveraging a

1195

