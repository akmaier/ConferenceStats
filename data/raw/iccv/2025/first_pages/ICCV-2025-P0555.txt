This ICCV paper is the Open Access version, provided by the Computer Vision Foundation.
Except for this watermark, it is identical to the accepted version;
the final published version of the proceedings is available on IEEE Xplore.

Hierarchical Variational Test-Time Prompt Generation for Zero-Shot
Generalization
Zhaoyang Wu Fang Liu∗ Licheng Jiao Shuo Li Lingling LiXu Liu Puhua Chen Wenping Ma
Key Laboratory of Intelligent Perception and Image Understanding of Ministry of Education
School of Artificial Intelligence, Xidian University, Xi’an 710071, China

Abstract

sify input images. At inference time, a handcrafted prompt
(e.g., “a photo of a <CLASS>” ) is used to query the
text encoder. However, efficiently adapting CLIP to specific
downstream tasks remains a significant challenge. While
fine-tuning CLIP on task-specific data can improve performance, it risks losing the model’s inherent zero-shot generalization [26]. Recent methods have explored prompt learning,
which adapts models without retraining, offering a more
flexible approach by learning task-specific prompts from
training data [8, 21, 22, 48, 51, 52].
Despite the success of prompt learning during training,
existing approaches treat both text and visual prompts as
fixed learnable parameters, limiting their adaptability to unseen domains. To address distribution shifts between training
and testing, test-time prompt tuning (TPT) [38] has been proposed, which dynamically adjusts text prompts at inference
by selecting high-confidence samples from augmented views
and minimizing entropy. However, TPT and other existing
methods fail to fully address two critical limitations: (1)
they rely on fixed prompt parameters, preventing the model
from dynamically adapting prompts to test-time data; (2)
they remain sensitive to template selection and data augmentation biases, which restricts their robustness across diverse
domains.
Variational inference [23] has shown great promise in addressing challenges related to zero-shot learning and domain
generalization. By treating model parameters probabilistically, variational inference enables more robust adaptation
to unseen tasks and domains [6, 7]. However, variational
inference has not yet been applied to prompt learning for
vision-language models. We propose it as a promising solution to address template sensitivity and bias introduced by
different prompt templates and augmentation views, offering
a more flexible framework for domain generalization.
To overcome the limitations outlined above, we introduce three key innovations: First, we propose a
HyperTransformer-based prompt generation mechanism that
dynamically generates both text and visual prompts at test
time. Unlike existing methods that treat prompts as fixed
learnable parameters, our approach enables data-specific

Vision-language models like CLIP have demonstrated strong
zero-shot generalization, making them valuable for various downstream tasks through prompt learning. However,
existing test-time prompt tuning methods, such as entropy
minimization, treat both text and visual prompts as fixed
learnable parameters, limiting their adaptability to unseen
domains. In contrast, we propose Hierarchical Variational
Test-Time Prompt Generation, a novel approach where both
text and visual prompts are dynamically generated via a
HyperTransformer at inference time. This enables the model
to produce data-specific prompts for each modality, significantly improving generalization. To further address template
sensitivity and distribution shifts, we introduce variational
prompt generation, leveraging variational inference to mitigate biases introduced by different prompt templates and
data augmentations. Additionally, our hierarchical variational prompt generation conditions prompts at each layer
on those from previous layers, allowing the model to capture deeper contextual dependencies and refine prompt interactions for robust adaptation. Extensive experiments on
domain generalization benchmarks demonstrate that our
method significantly outperforms existing prompt-learning
techniques, achieving state-of-the-art zero-shot accuracy
while maintaining efficiency.

1. Introduction
Foundation models that span multiple modalities, such as vision, language, and audio, have proven to be highly effective
in a wide range of downstream applications. Among these
models, Vision-Language (V-L) models like CLIP (Contrastive Language-Image Pretraining) [35], which has been
pre-trained on large-scale image-text pairs, have demonstrated impressive zero-shot generalization abilities, particularly for tasks like image classification. CLIP utilizes parallel
vision and language encoding branches, with the similarity
between the embeddings from both branches used to clas-

2325

