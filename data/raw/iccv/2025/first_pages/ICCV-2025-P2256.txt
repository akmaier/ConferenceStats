This ICCV paper is the Open Access version, provided by the Computer Vision Foundation.
Except for this watermark, it is identical to the accepted version;
the final published version of the proceedings is available on IEEE Xplore.

MDP-Omni: Parameter-free Multimodal Depth Prior-based Sampling
for Omnidirectional Stereo Matching
∗

Eunjin Son1

HyungGi Jo1
Wookyong Kwon2
Sang Jun Lee1
1
Jeonbuk National University, Republic of Korea
2
Electronics and Telecommunications Research Institute (ETRI), Republic of Korea
{eunjinson, hygijo, sj.lee}@jbnu.ac.kr

wkwon@etri.re.kr

Abstract
Omnidirectional stereo matching (OSM) estimates 360◦
depth by performing stereo matching on multi-view fisheye
images. Existing methods assume a unimodal depth distribution, matching each pixel to a single object. However, this
assumption constrains the sampling range, causing oversmoothed depth artifacts, especially at object boundaries.
To address these limitations, we propose MDP-Omni, a
novel OSM network that leverages parameter-free multimodal depth priors. Specifically, we design a sampling
strategy that adaptively adjusts the sampling range based
on a multimodal probability distribution, without introducing any additional parameters. Furthermore, we present the
azimuth-based multi-view volume fusion module to build a
single cost volume. It mitigates false matches caused by
occlusions in warped multi-view volumes. Experimental
results demonstrate that MDP-Omni significantly improves
existing methods, particularly in capturing fine details.

Figure 1. Comparison between the previous and proposed sampling methods. (a) Visualization of the adjusted sampling range,
where the unimodal approach (top) limits sampling to a local
range, while our method (bottom) expands the range to capture
multiple matchable objects. (b) GT index (red), predicted index
(green), and sampled index hypotheses (blue) for pixels indicated
by yellow dotted lines in the inverse depth map. (c) Predicted inverse depth maps. (d) Corresponding error maps.

1. Introduction

capturing the 360◦ scene with fewer cameras, which makes
OSM a preferred method for omnidirectional depth estimation [4, 5, 12, 18, 25, 27].
Learning-based MVS algorithms build a cost volume by
warping source images to the reference view and computing
matching costs for possible depth hypotheses. The optimal
depth is then estimated by predicting a probability distribution over these hypotheses. Similarly, OSM constructs
a cost volume through spherical sweeping [10], projecting
multi-view images onto a series of concentric spheres with
varying radii. These spheres are sampled on an inverse
depth scale within a predefined depth range. To achieve
high accuracy, previous OSM works [4, 5, 12, 27] employ dense sampling, but this approach requires substantial
memory, especially for 3D CNN-based cost regularization.
Cascade cost volume-based architectures [3, 8, 19] are
widely used in MVS for memory-efficient cost regularization. This framework predicts depth at each stage using L1
loss and adjusts the sampling range to a local range centered on the prediction. This supervision enforces a uni-

Stereo matching is a task for estimating depth by identifying correspondences between stereo image pairs. This
task enables 3D reconstruction from 2D images, making
it essential for 3D perception. However, previous works
have mainly focused on images with a narrow field of view
(FOV), such as stereo pairs [7, 16, 17, 20] or object-centric
multi-view images [11, 13, 32]. Hence, these standard approaches are not well-suited for capturing omnidirectional
depth, which limits their applicability in real-world scenarios, such as autonomous driving.
To estimate a depth map for a 360◦ scene, omnidirectional stereo matching (OSM) employs multi-view stereo
(MVS) on fisheye images captured by a multi-camera rig
facing the front, right, back, and left directions. These images are warped into rig space, which serves as a reference
view for depth representation. This configuration allows

26178

