This ICCV paper is the Open Access version, provided by the Computer Vision Foundation.
Except for this watermark, it is identical to the accepted version;
the final published version of the proceedings is available on IEEE Xplore.

PEFTDiff: Diffusion-Guided Transferability Estimation for Parameter-Efficient
Fine-Tuning
Prafful Kumar Khoba1 Zijian Wang2 Chetan Arora3 Mahsa Baktashmotlagh2
1
UQ–IITD Research Academy, New Delhi, India
2
The University of Queensland, Brisbane, Australia
3
Indian Institute of Technology Delhi, New Delhi, India
qiz228274@iitd.ac.in, zijian.wang@uq.edu.au, chetan@cse.iitd.ac.in, m.baktashmotlagh@uq.edu.au

Abstract

🔥Trainable

PEFT Pool

❄ Frozen

Predicted Rank

⚠Time-intensive

Selecting an optimal Parameter-Efficient Fine-Tuning
(PEFT) technique for a downstream task is a fundamental challenge in transfer learning. Unlike full fine-tuning,
where all model parameters are updated, PEFT techniques
modify only a small subset of parameters while keeping
the backbone frozen, making them computationally efficient.
However, this introduces a unique problem: selecting the
most effective PEFT method for a given dataset. Existing
transferability estimation (TE) metrics primarily focus on
ranking distinct architectures and struggle to detect subtle
embedding differences introduced by various PEFT methods sharing the same backbone. To address this limitation, we propose a novel diffusion-based metric explicitly
designed for PEFT selection. Unlike conventional metrics, our approach models the fine-grained geometric relationships of embedding spaces through a diffusion process,
effectively quantifying intra-class compactness and interclass separability. Extensive evaluations on the VTAB1k benchmark validate our method’s effectiveness, demonstrating a substantial 68.95% improvement over LogME,
1297.29% over N LEEP, 149.75% over NCTI, 135% over
GBC, and 140.46% over SFDA—five widely used TE methods designed for ranking pre-trained models. The code
is available at https://prafful-kumar.github.
io/peftdiff.github.io/

2

Fine-tuning

4

👍

Target data

1

PEFT selection
method
⏳Efficient

Target Dataset

Target Dataset

PEFT selection

Traditional TE
ResNet

Dense
Net

3

VGG

VPT

ViT-B

LoRA

ViT-B

Figure 1. The top section highlights the need for PEFT selection by comparing it with fine-tuning. Fine-tuning (red path) is
time-intensive, whereas our selection method (green path) efficiently ranks PEFT techniques. The bottom section highlights
the distinction between traditional TE and PEFT selection. Traditional TE evaluates different backbone architectures (e.g., ResNet,
DenseNet, VGG) for a given task, whereas PEFT selection ranks
different fine-tuning strategies (e.g., LoRA, VPT) while keeping
the backbone fixed.

one for a given task is a nontrivial challenge. Fine-tuning
each PEFT method is computationally expensive, making
exhaustive evaluation impractical. Therefore, an efficient
PEFT selection strategy is crucial for informed decisionmaking without excessive resource consumption.
Transferability estimation (TE) [1] is the most closely
related research area to PEFT selection, aiming to predict
how well a pre-trained model can adapt to a new downstream task without requiring extensive fine-tuning. TE
methods typically assign a surrogate score to quantify the
suitability of a model for a given task. Early TE studies

1. Introduction
Parameter-Efficient Fine-Tuning (PEFT) techniques [12,
15] enable efficient adaptation of large-scale pre-trained
vision models (PVMs) [22] by fine-tuning only a small
subset of parameters, significantly reducing computational
costs while preserving pre-trained knowledge. Given the
increasing number of PEFT techniques [4, 14, 16, 19–
21, 31, 34, 38, 40, 41, 43], selecting the most suitable

1454

