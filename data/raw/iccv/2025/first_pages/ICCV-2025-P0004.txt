This ICCV paper is the Open Access version, provided by the Computer Vision Foundation.
Except for this watermark, it is identical to the accepted version;
the final published version of the proceedings is available on IEEE Xplore.

Gradient Decomposition and Alignment for Incremental Object Detection
Wenlong Luo 1 Shizhou Zhang*1 De Cheng*2 Yinghui Xing1 Guoqiang Liang1
Peng Wang1 Yanning Zhang1
1
Northwestern Polytechnical University, China
2
Xidian University, China
luowenlong@mail.nwpu.edu.cn {szzhang, xyh 7491, gqliang, peng.wang, ynzhang}@nwpu.edu.cn dcheng@xidian.edu.cn

Abstract

in real-world scenarios, new object categories often appear
over time. This dynamic setting requires object detection
models to continuously adapt to these new categories, while
also addressing challenges like limited storage for all past
data and data privacy concerns. To address this, incremental object detection (IOD) [38] has thus emerged as an essential approach, inspired by biological visual systems that
continuously acquire, integrate, and reorganize knowledge.
IOD enables models to recognize new objects as they appear without forgetting knowledge of learned categories.
The existing common way to solve IOD is through
knowledge distillation [8, 11, 14, 27, 32, 34, 55]. Researches have shown that using feature/prediction distillation can significantly reduce catastrophic forgetting in these
models. In one implementation [11], incremental learning
was applied to an anchor-free detector, where knowledge
distillation selectively focused on non-regression outputs,
effectively preserving previously learned knowledge while
adapting to new classes. However, most methods overlook
the unique challenge in IOD, foreground objects identified
in earlier stages are left unlabeled in later stages, which can
cause the model to misclassify them as background. While
distillation-based approaches emphasize strong regularization to prevent catastrophic forgetting, they fail to address
class overlap, leading to suboptimal performance on both
previously learned and new classes.
To address the challenges of catastrophic forgetting in
incremental object detection, we propose a comprehensive framework featuring a novel gradient decomposition
and alignment method. This method is built on an advanced pseudo-labeling strategy to address missing annotations of previously learned class objects in current training
images, effectively serving as a knowledge replay mechanism. Specifically, it employs a Gaussian Mixture model to
accurately estimate pseudo-labels and applies adaptive gradient alignment with these previously learned class objects,
enabling parameter updates that preserve prior knowledge
while promoting positive backward transfer.
Specifically, accurately estimating the pseudo old-class

Incremental object detection (IOD) is crucial for enabling
AI systems to continuously learn new object classes over
time while retaining knowledge of previously learned categories, allowing model to adapt to dynamic environments
without forgetting prior information. Existing IOD methods primarily employ knowledge distillation to mitigate
catastrophic forgetting, yet these approaches overlook class
overlap issues, often resulting in suboptimal performance.
In this paper, we propose a novel framework for IOD
that leverages a decoupled gradient alignment technique
on top of the specially proposed pseudo-labeling strategy.
Our method employs a Gaussian Mixture Model to accurately estimate pseudo-labels of previously learned objects in current training images, effectively functioning as
a knowledge-replay mechanism. This strategy reinforces
prior knowledge retention and prevents the misclassification of unannotated foreground objects from earlier classes
as background. Furthermore, we introduce an adaptive
gradient decomposition and alignment method to maintain
model stability while facilitating positive knowledge transfer. By aligning gradients from both old and new classes,
our approach preserves previously learned knowledge while
enhancing plasticity for new tasks. Extensive experiments
on two IOD benchmarks demonstrate the effectiveness of
the proposed method, achieving superior performances to
state-of-the-art methods. The code and datasets are available at https://github.com/FHR-L/GDA-IOD.

1. Introduction
Object detection is a fundamental computer vision task,
driving progress in numerous specialized areas. Existing
general object detection models [9, 10, 40, 42] assume access to all target classes and fully labeled data from the start,
with a fixed data distribution throughout training. However,
* Corresponding author.

4486

