This ICCV paper is the Open Access version, provided by the Computer Vision Foundation.
Except for this watermark, it is identical to the accepted version;
the final published version of the proceedings is available on IEEE Xplore.

MambaML: Exploring State Space Models for Multi-Label Image Classification
Xuelin Zhu1 Jian Liu2 Jiuxin Cao3 Bing Wang1 *
1
The Hong Kong Polytechnic University 2 Ant Group 3 Southeast University
{xuelin.zhu, bingwang}@polyu.edu.hk, rex.lj@antgroup.com, jx.cao@seu.edu.cn

Abstract

label-specific representations

Mamba, a selective state-space model, has recently seen
widespread application across various visual tasks due to
its exceptional ability to capture long-range dependencies.
While promising results have been demonstrated in image
classification, its potential in multi-label image classification remains underexplored. To bridge this gap, we propose a novel Mamba-based decoder, which utilizes the intrinsic attention of Mamba to aggregate visual information
from image features into label embeddings, yielding labelspecific visual representations. Building upon this, a MambaML framework is developed for multi-label image classification, which models the self-correlations of image features and label embeddings with bi-directional Mamba, as
well as their cross-correlations with the Mamba-based decoder, allowing visual spatial relationships, label semantic dependencies, and cross-modal associations to be explored in a unified system. In this way, robust label-specific
visual representations are acquired, facilitating the training of binary classifiers towards accurate label recognition.
Experiments on public benchmarks suggest that our MambaML achieves performance comparable to state-of-the-art
methods in multi-label image classification, while requiring
fewer parameters and computational overhead.

feature maps

Decoder

×𝐿

Bi-directional Mamba

Bi-directional Mamba
Mamba-based
cross-attention

learnable query tokens

Figure 1. A brief illustration of the proposed MambaML framework. It incorporates a bi-directional Mamba to enhance the convolutional feature maps with the global context of the input image, and a Mamba-based decoder to acquire label-specific visual
representations for classification. The decoder is instantiated by
stacking multiple layers of Mamba-based cross-attention module
and bidirectional Mamba module. The former aggregates visual
information from the feature maps into the query tokens, while the
latter captures semantic dependencies among these tokens.

One of the central challenges in multi-label image classification is effectively modeling the complex correlations
among labels. Existing approaches can be broadly divided
into three categories: those based on recurrent neural networks (RNNs) [4, 49, 58], those utilizing graph networks
[6, 8, 51, 59, 67], and those employing multi-head selfattention (MHA) [9, 33, 37, 66, 68]. While promising performance has been achieved, these studies face substantial
challenges in terms of time and computational efficiency. In
particular, RNN-based and graph network-based methods
suffer from the time-consuming process of iterative propagation, whereas MHA-based approaches are burdened by
quadratic complexity concerning image size. These inherent limitations motivate the development of an innovative
architecture that offers linear computational complexity and
parallel acceleration capability, thereby enhancing the efficiency of multi-label image classification.

1. Introduction
Multi-label image classification is one of the fundamental
tasks in computer vision, which requires identifying multiple objects from a input image. Unlike single-label classification, where only one salient object is identified, multilabel classification faces the challenge of dealing with images containing various objects and combinations. This
complexity makes the task more intractable. Multi-label
image classification has garnered sustained research enthusiasm for many years due to its importance in a wide range
of downstream applications, such as scene understanding
[44] and human attribute recognition [35].
* Corresponding author.

4743

