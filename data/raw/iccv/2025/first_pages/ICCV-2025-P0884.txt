This ICCV paper is the Open Access version, provided by the Computer Vision Foundation.
Except for this watermark, it is identical to the accepted version;
the final published version of the proceedings is available on IEEE Xplore.

OV3D-CG: Open-vocabulary 3D Instance Segmentation with Contextual
Guidance

1

Mingquan Zhou1,2 Chen He1 Ruiping Wang1,2 * Xilin Chen1,2
Institute of Computing Technology, Chinese Academy of Sciences, China
2
University of Chinese Academy of Sciences, China

mingquan.zhou@vipl.ict.ac.cn, {hechen,wangruiping,xlchen}@ict.ac.cn

Abstract

3D point cloud

Existing methods
Top K views cropped

Open-vocabulary 3D instance segmentation (OV-3DIS),
which aims to segment and classify objects beyond predefined categories, is a critical capability for embodied AI applications. Existing methods rely on pre-trained 2D foundation models, focusing on instance-level features while
overlooking contextual relationships, limiting their ability
to generalize to rare or ambiguous objects. To address these
limitations, we propose an OV-3DIS framework guided by
contextual information. First, we employ a Class-agnostic
Proposal Module, integrating a pre-trained 3D segmentation model with a SAM-guided segmenter to extract robust 3D instance masks. Subsequently, we design a Semantic Reasoning Module, which selects the best viewpoint for each instance and constructs three 2D contextaware representations. The representations are processed
using Multimodal Large Language Models with Chain-ofThought prompting to enhance semantic inference. Notably, our method outperforms state-of-the-art methods on
the ScanNet200 and Replica datasets, demonstrating superior open-vocabulary segmentation capabilities. Moreover, preliminary implementation in real-world scenarios
verifies our method’s robustness and accuracy, highlighting
its potential for embodied AI tasks such as object-driven
navigation. Our project page is at: https://viplvsu.github.io/OV3D-CG/.

“container”
CLIP
Image
encoder
“cabinet”
Instance
Segmenter
3D class-agnostic
proposal

OV3D-CG
Best viewpoint
Context-aware representation

CoT
Prompt

“computer
tower”

“file
cabinet”

Figure 1. Comparison of existing methods and our method for
3D instance segmentation. Existing methods rely on CLIP, focusing mainly on object-level information from local multi-view
perspectives, which can lead to ambiguous or incorrect labels. Our
method selects the most visible view, incorporates context-aware
representations, and uses an MLLM with CoT prompting, resulting in more accurate object segmentation.

ing training. This limitation highlights the need for openvocabulary 3D instance segmentation (OV-3DIS) to handle
the diversity of objects in real-world scenes.
Recent OV-3DIS methods, such as OpenMask3D [32]
and Open3DIS [24], use pre-trained 3D models for classagnostic mask generation and CLIP [27] for zero-shot instance recognition. However, these methods primarily rely
on instance-level 2D projections, failing to capture broader
contextual information crucial for accurate object recognition. Additionally, CLIP can only establish associations between isolated objects and textual semantics, limiting its
ability to leverage contextual relationships within a scene.
For objects lacking distinct visual cues, contextual reasoning becomes even more critical. For example, a “computer
tower” may be recognized as a “container” without incor-

1. Introduction
3D instance segmentation is critical for applications such
as robotics, autonomous driving, and augmented reality,
where precise interaction with 3D environments is essential.
This task involves detecting instances in a 3D point cloud,
segmenting foreground points, and assigning each a semantic label. While fully supervised methods have made significant progress [4, 13, 15, 23, 30, 36], they struggle with
generalization, especially to unseen object categories dur* Corresponding author

5305

