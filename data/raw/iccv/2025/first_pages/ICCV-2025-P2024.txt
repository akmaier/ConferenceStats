This ICCV paper is the Open Access version, provided by the Computer Vision Foundation.
Except for this watermark, it is identical to the accepted version;
the final published version of the proceedings is available on IEEE Xplore.

LeGrad: An Explainability Method for Vision Transformers via Feature
Formation Sensitivity
Walid Bousselham1
1

Angie Boggust2

Sofian Chaybouti1
2

Hendrik Strobelt3,4
3

Hilde Kuehne1,3

4

University of Tuebingen & Goethe University Frankfurt, MIT CSAIL, MIT-IBM Watson AI Lab IBM Research,

Dog

Abstract

Shoe

1. Introduction

Rollout

CheferCAM

TextSpan

LeGrad

Vision Transformers (ViTs) have become a standard architecture in computer vision. However, because of their
modeling of long-range dependencies through self-attention
mechanisms, the explainability of these models remains a
challenge. To address this, we propose LeGrad, an explainability method specifically designed for ViTs. LeGrad computes the gradient with respect to the attention maps of single ViT layers, considering the gradient itself as the explainability signal. We aggregate the signal over all layers, combining the activations of the last as well as intermediate tokens to produce the merged explainability map. This makes
LeGrad a conceptually simple and an easy-to-implement
method to enhance the transparency of ViTs. We evaluate
LeGrad in various setups, including segmentation, perturbation, and open-vocabulary settings, showcasing its improved spatial fidelity and its versatility compared to other
SotA explainability methods. A demo and the code is available at https://walidbousselham.com/LeGrad.

Tattoo

Raw Attention

Vision Transformers (ViTs) [13] have transformed computer vision by leveraging self-attention mechanisms to
capture long-range dependencies. However, explaining
their predictions poses significant challenges, as traditional methods like GradCAM [33], which relies on convolutional layers, and Layer-wise Relevance Propagation
(LRP) [2], tailored to feed-forward networks, are ill-suited
to ViTs’ distinct architecture. Although ViT-specific techniques—such as adaptations of traditional approaches [9,
10, 33], attention-based methods [1, 9, 10, 38], and textbased explanations [1, 18, 22]—have emerged, they often
fail to deliver reliable and interpretable insights. These
methods typically suffer from three key limitations: (1) they
produce explanations with significant background noise due
to their reliance on raw attention patterns (Fig.1), (2)struggle to filter out the influence of ”register tokens” that accumulate high attention weights despite lacking semantic relevance[12], and (3)fail to establish a direct connec-

Figure 1. Qualitative comparison of explainability heatmaps
generated by LeGrad and state-of-the-art methods: All baselines exhibit identical background artifacts (pink circles), revealing a shared limitation due to their reliance on raw attention patterns. In contrast, LeGrad, leveraging gradient-based sensitivity,
effectively suppresses these artifacts, yielding heatmaps that more
accurately highlight decision-critical regions of the target objects.

20336

tion between attention patterns and the model’s final decision process. Our analysis further reveals that different ViT models activate unique subsets of layers, forming
model-specific “fingerprints” in their feature formation process, see Fig. 6. This variability necessitates an approach

