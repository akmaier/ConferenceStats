This ICCV paper is the Open Access version, provided by the Computer Vision Foundation.
Except for this watermark, it is identical to the accepted version;
the final published version of the proceedings is available on IEEE Xplore.

Q-Norm: Robust Representation Learning via Quality-Adaptive Normalization
Lanning Zhang1,∗ , Ying Zhou2, *, Fei Gao1,† , Ziyun Li3 , Maoying Qiao4 , Jinlan Xu2 , Nannan Wang1,†
1
Xidian University, China
2
Hangzhou Dianzi University, China
3
KTH Royal Institute of Technology, Sweden
4
University of Technology Sydney (UTS), Australia
zhanglanning@stu.xidian.edu.cn; yyzhou@hdu.edu.cn; {fgao, nnwang}@xidian.edu.cn

Abstract
Although deep neural networks have achieved remarkable success in various computer vision tasks, they face significant challenges in degraded image understanding due
to domain shifts caused by quality variations. Drawing biological inspiration from the human visual system (HVS),
which dynamically adjusts perception strategies through
contrast gain control and selective attention to salient
regions, we propose Quality-Adaptive Normalization (QNorm) - a novel normalization method that learns adaptive
parameters guided by image quality features. Our approach
addresses two critical limitations of conventional normalization techniques: 1) Domain Covariance Shift: Existing methods fail to align feature distributions across different quality domains. Q-Norm implicitly achieves crossdomain alignment through quality-aware parameter adaptation without explicit loss functions. 2) Biological Plausibility: By mimicking HVS’s contrast normalization mechanisms and attention-based feature selection, Q-Norm dynamically adjusts the mean and variance parameters using
a pre-trained quality assessment model, ensuring robustness to image degradation. Extensive experiments across
multiple tasks (image classification, semantic segmentation, object detection) demonstrate that Q-Norm consistently outperforms baseline methods on low-quality images.
Code has been released at https://github.com/
IIP-Lab-XDU/Q-Norm.

KLD ( HQ | LQ )
0.0946
↓

0.0449

Figure 1. The feature distribution of high-quality(HQ) and its
degraded low-quality(LQ) after normalization. The distributions
draw closer after Q-Norm.

quality (LQ) images degraded by fog, rain, snow, or low
light [11]. Such degradations obscure fine details, reduce
contrast, and introduce noise [4, 12, 13, 57, 67], disrupting feature extraction and recognition [6, 49]. This performance degradation stems from a fundamental distribution
shift between HQ and LQ images. Improving performance
on low-quality images remains a fundamental challenge in
computer vision [15, 32, 39, 70].

1. Introduction

A natural approach to mitigating distribution shifts between HQ and LQ images is normalization, which adjusts feature statistics to improve stability and generalization. Normalization layers are fundamental components
in deep neural networks, helping to accelerate training
and improve robustness by aligning feature distributions
[2, 20, 22, 61, 65]. Early methods such as Batch Normalization (BN) [22] address internal covariate shift by normalizing feature statistics across mini-batches, while Layer
Normalization (LN) [2], Group Normalization (GN) [65],
and Instance Normalization (IN) [61] introduce alternative

Deep learning has achieved remarkable success in image
classification [19, 40], object detection [12, 28, 37, 52, 55,
69], and segmentation [27, 29, 35]. However, these models
are primarily trained on high-quality (HQ) images, resulting in a substantial performance drop when processing low* Equal contributions. † Corresponding authors

13901

