This ICCV paper is the Open Access version, provided by the Computer Vision Foundation.
Except for this watermark, it is identical to the accepted version;
the final published version of the proceedings is available on IEEE Xplore.

DecAD: Decoupling Anomalies in Latent Space for Multi-Class Unsupervised
Anomaly Detection
Xiaolei Wang1,2

Xiaoyang Wang4 Huihui Bai3 Eng Gee Lim1 Jimin Xiao1 *
1
2
XJTLU
University of Liverpool
3
Tangshan Research Institute of BJTU 4 Shanghai AI Laboratory

Abstract
Normal Manifold

Existing distillation-based and reconstruction-based methods have a critical limitation: Autoencoder-based frameworks trained exclusively on normal samples unexpectedly
well reconstruct abnormal features, leading to degraded
detection performance. We identify this phenomenon as
‘anomaly leakage’ (AL): the decoder optimized by reconstruction or distillation loss tends to directly copy the encoded input, regardless of whether the input is a normal
or abnormal feature. To address this issue, we propose a
novel framework that explicitly decouples encoded features
into normal and abnormal components through a special
invertible mapping in a prior latent space. Next, we remove
abnormal components and leverage normal remainders for
feature reconstruction. Compared to previous methods, the
invertible structure can eliminate anomalous information
point-to-point without damaging the information of neighboring patches, improving reconstruction. In this process,
effective synthetic abnormal features are essential for training the decoupling process. Therefore, we propose applying adversarial training to find suitable perturbations to
simulate feature-level anomalies. Extensive experimental
evaluations on benchmark datasets, including MVTec AD,
VisA, and Real-IAD, demonstrate that our method achieves
competitive performance compared to state-of-the-art approaches. Code is available at DecAD.

Normal Feature
New Feature

New Feature

Latent Space

Testing Feature

Synthetic Anomaly
Feature

Invertible Mapping

Normal Channels

Reconstruction Loss

Abnormal Channels

Figure 1. Our motivation is to adopt an invertible mapping to decouple the encoded feature into normal and abnormal parts in the
latent space (the blue region), removing the abnormal component
and leaving the normal component for decoding. To learn this
process during training, we propose the adversarial perturbation to
synthesize the feature-level anomalies. The gray dashed line represents regions outside the normal manifold.

1. Introduction

Recent advances in unsupervised AD primarily focus
on two similar methodological streams: feature reconstruction [17, 18, 52] and feature reverse-distillation frameworks
[9, 41, 44]. These approaches typically employ a learnable decoder to reconstruct features from a frozen encoder
by minimizing the cosine similarity or distance between
patches. In principle, a decoder trained exclusively on normal data should successfully reconstruct normal patch features while failing to reproduce anomalous patterns. This
characteristic enables anomaly localization through comparison of encoded and decoded patch features. However, the decoder optimized by reconstruction or distillation
loss tends to directly copy the encoded input, regardless of
whether the input is a normal or abnormal feature. We term
the phenomenon as ‘anomaly leakage’ (AL), which substantially compromises detection performance.

Industrial visual anomaly detection (AD) represents an important application scenario in intelligent manufacturing. It
focuses on automated identification of unexpected defects
such as damages, scratches, and misplacement on product
surfaces. Given the inherent unpredictability and diverse
nature of potential anomalies, unsupervised approaches that
require only normal samples for training have emerged as
the predominant solution.
* Corresponding Author

21568

