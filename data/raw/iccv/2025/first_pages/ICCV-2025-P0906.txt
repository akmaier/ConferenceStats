This ICCV paper is the Open Access version, provided by the Computer Vision Foundation.
Except for this watermark, it is identical to the accepted version;
the final published version of the proceedings is available on IEEE Xplore.

GloPER: Unsupervised Animal Pattern Extraction from Local Reconstruction
Bowen Chen Yun Sing Koh Gillian Dobbie
The University of Auckland, New Zealand
bche264@aucklanduni.ac.nz

y.koh@auckland.ac.nz

g.dobbie@auckland.ac.nz

Abstract
Traditional image segmentation methods struggle with finegrained pattern extraction, especially in an unsupervised
setting without labeled data. Shallow and deep learning
approaches either lack structural coherence or focus on
object-level segmentation rather than internal textures. Additionally, existing methods often fail to generalize across
diverse animal species due to variations in pattern complexity and lighting variations. We introduce GloPER, an
unsupervised segmentation framework that extracts finegrained animal patterns without labeled supervision. By
enforcing local image reconstruction with only two colors per region, GloPER captures structured patterns while
mitigating the effects of shadows and lighting inconsistencies. Given the lack of fine-detailed labeled data, we construct a dataset of 10 animal species, each with at least
100 well labeled images, enabling direct segmentation assessment. Experimental results show that GloPER outperforms both shallow and deep segmentation baselines, with
a 42.44% higher DICE score on average across all 10 animal species. We also assess its effectiveness through animal
re-identification (ReID), where GloPER’s extracted binary
patterns achieve superior accuracy, in some cases exceeding full-image ReID performance, underscoring the discriminative power of structured segmentation.

Figure 1. Challenges in Fine-grained Animal Pattern Extraction. These examples highlight how the intrinsic color of animal
patterns can shift as well as how shadows and illumination affect
pattern appearance; GloPER overcomes these issues and achieves
robust unsupervised pattern segmentation.

capturing fine-grained internal patterns are scarce. Unlike
bounding boxes, which take seconds to annotate, pixelaccurate segmentation of textures such as fur, scales, or skin
markings is costly and time-consuming, often requiring domain expertise. As a result, existing segmentation models
prioritize object separation by default, making them largely
ineffective at capturing intricate biological structures without explicit supervision.

1. Introduction

Several alternative strategies have attempted to circumvent the need for detailed annotations. Weakly supervised
methods [21, 48, 51] rely on partial labels but struggle with
fine detail capture. Self-supervised and contrastive learning approaches [6, 32, 46] focus on object-level grouping
and fail to model intricate textures. Vision-language models (VLMs) [24, 35, 50] rely on textual descriptions that
are effective for object distinction but fail to generalize to
species-specific patterns, as fur textures, unlike object labels such as “zebra” or “dog”, lack universal textual representations. To address these challenges, we focus on unsupervised segmentation of animal patterns, leveraging structured assumptions about color and spatial continuity. Un-

Image segmentation is a fundamental task in computer vision, widely applied in autonomous driving [29, 38], medical imaging [3, 5], and object detection [23]. While most
segmentation methods focus on object-background separation, many applications require fine-grained segmentation
of internal structures, such as leaf venation [16] or fur patterns [31], which are important for species classification and
behavioral studies. Despite its significance, fine-grained
segmentation remains underexplored due to dataset limitations and model biases toward object-level features. Largescale datasets such as COCO [25] and Pascal VOC [10]
provide object-level segmentation annotations, but datasets

6519

