This ICCV paper is the Open Access version, provided by the Computer Vision Foundation.
Except for this watermark, it is identical to the accepted version;
the final published version of the proceedings is available on IEEE Xplore.

Memory-Efficient Generative Models via Product Quantization
Jie Shao
Hanxiao Zhang
Hao Yu
Jianxin Wu*
National Key Laboratory for Novel Software Technology, Nanjing University, China
School of Artificial Intelligence, Nanjing University, China
{shaoj, zhanghx, yuh}@lamda.nju.edu.cn, wujx2001@nju.edu.cn
Abstract
*
O

The rapid progress in generative models has significantly enhanced the quality of image generation. However, as these models grow larger, deploying and fine-tuning
them becomes increasingly challenging. While conventional quantization techniques help reduce model size, they
struggle to achieve high compression rates without significant performance loss. As a result, memory footprint remains a critical challenge for generative models. In this
work, we explore the extreme compression of generative
models through codebook quantization, drastically reducing model size while maintaining performance. We extend
product quantization for model compression, significantly
increasing codebook capacity, which is crucial for preserving the generative quality of diffusion models. We also introduce a codebook compression method for memory efficiency. To further minimize performance degradation, we
develop EM calibration with re-initialization that optimizes
both assignments and centroids. By compressing the model
to as low as 1 bit (achieving a 13→ reduction in model
size), we obtain a highly compact generative model with
remarkable image quality. Extensive experiments on ImageNet demonstrate the superiority of our method over existing techniques. Furthermore, we validate its effectiveness
across various generation, language and 3D tasks, highlighting its broad applicability and robust performance.

*

16

Figure 1. Compression results at low bit-widths. PTQ methods
(top right) exhibit significant performance degradation at low bitwidths, whereas our method demonstrates superior performance at
1–2 bits. * indicates our implemented version and baseline model
is DiT-XL/2 (step=250, cfg=1.5)

1. Introduction

lenges of diffusion models in resource-constrained environments, compression techniques, particularly quantization [5, 32, 51], play a vital role in reducing model size
and accelerating inference. By converting weights and activations from floating-point to lower precision formats, 4–8
bits post-training quantization (PTQ) achieves a balance between compression effectiveness and performance.
However, most PTQ methods suffer from severe degradation at extremely low bit-widths (e.g., 1–2 bits shown
in Fig. 1), restricting the compression ratio to at most 4→
(assuming an FP16 baseline). This limitation makes them
far from sufficient for model compression given the rapid
growth in model size. A realistic challenge is that many
users struggle to access the SOTA generative models due
to memory limitations. Even after quantization, which may
facilitate inference, training or fine-tuning these models remains difficult. On mobile devices, frequent loading from
ROM to RAM also remains a bottleneck due to the large
model size. Thus, further reducing the memory footprint
of generative models is crucial for real-world applications
— a challenge that existing quantization and compression
methods have yet to fully address.

Deep learning has achieved remarkable success across a
wide range of tasks [3, 18, 46]. In particular, recent advancements in diffusion models have enabled the generation
of high-quality images [22, 55], videos [2, 68], and other
contents [6, 45]. However, despite their impressive performance, the extensive model size and computational overhead make them highly resource-intensive, limiting their
practicality for real-world applications. To address the chal* Corresponding author.

16871

