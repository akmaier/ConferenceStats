This ICCV paper is the Open Access version, provided by the Computer Vision Foundation.
Except for this watermark, it is identical to the accepted version;
the final published version of the proceedings is available on IEEE Xplore.

Think Twice: Test-Time Reasoning for Robust CLIP Zero-Shot Classification
Shenyu Lu Zhaoying Pan Xiaoqian Wang*
Elmore Family School of Electrical and Computer Engineering
Purdue University
{lu876, pan433, joywang}@purdue.edu

Abstract
Contrastive Language-Image Pre-training (CLIP) models
exhibit intriguing properties, particularly in their zero-shot
classification capability. However, the reliability of CLIP
zero-shot classification is severely undermined by spurious
correlations. Existing efforts to enhance the robustness of
zero-shot CLIP models often rely on prior knowledge or annotations of spurious correlations, limiting real-world applicability due to the unavailability of such information.
Alternative methods attempt to detect distribution shift at
test time but require training statistics whose access is often restricted or computationally expensive. To address the
challenges brought by spurious correlation under zero-shot
settings, we propose a novel test-time reasoning approach.
Our method, inspired by human recognition, localizes the
object and refines the classification accordingly. The inherent capacity of CLIP for semantic understanding allows
us to isolate the object of interest without auxiliary models. Zero-shot classification is then performed exclusively
on the localized objects, effectively mitigating the influence
of spurious correlation. The proposed approach is interpretable and flexible as it requires no spurious annotations
or prior knowledge, making it widely applicable. The substantial improvements across multiple benchmark datasets
validated the effectiveness of our approach.

Figure 1. Attention map visualization based on [CLS] for randomly selected samples. Left: Vanilla zero-shot prediction with
different vision transformers. Right: Proposed method applied to
the same vision transformers.

tion occurs when a feature is correlated with the target label
but lacks a causal relationship [19]. This issue is particularly critical in high-stakes settings where reliance on such
correlations can lead to detrimental consequences. For instance, Seyyed-Kalantari et al. [43] found that pneumonia
diagnosis systems using chest X-rays may mistakenly base
their predictions on patient demographics rather than the actual medical conditions, highlighting the potential risks of
these correlations.
Existing works have attempted to address spurious correlations in zero-shot CLIP models, with many focusing
on debiasing the text space. However, as noted by [31],
these approaches require domain expertise to identify spurious features and carry the risk of causing misalignment
with the image modality. Alternative methods, such as
ROBOSHOT [1] and TIE* [31], propose robust solutions
but inherently rely on prior identification and annotations
of spurious features, which significantly restricts their applicability. As different spurious text prompts yield varying robustness outcomes, identifying the optimal spurious
prompts remains challenging. More importantly, the effectiveness of these methods is often compromised in realworld scenarios where spurious features are not predefined,

1. Introduction
Contrastive Language-Image Pre-training (CLIP) [38] has
achieved significant success as a powerful pre-training
paradigm. It leverages a unified multimodal framework that
integrates images and text to achieve strong generalizability.
The zero-shot capability of CLIP allows it to classify unseen test samples, making it highly applicable in real-world
scenarios. Despite its impressive generalizability, CLIP remains susceptible to spurious correlations, undermining the
reliability of its zero-shot predictions. A spurious correla* Corresponding author.

2919

