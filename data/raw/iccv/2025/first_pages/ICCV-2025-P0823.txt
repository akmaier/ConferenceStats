This ICCV paper is the Open Access version, provided by the Computer Vision Foundation.
Except for this watermark, it is identical to the accepted version;
the final published version of the proceedings is available on IEEE Xplore.

Hierarchical Divide-and-Conquer Grouping for Classiﬁcation
Adaptation of Pre-Trained Models
Ziqian Lu1
Yunlong Yu2 *
1
Zhejiang Sci-Tech University

Qinyue Tong2
Jun Liu2
2
Zhejiang University

ziqianlu@zstu.edu.cn, {yuyunlong, qinyuetong,junliu0930}@zju.edu.cn

Abstract

distributions that deviate signiﬁcantly from those encountered during training. As shown in Fig. 1(a), classifying
both base and novel classes within a uniﬁed space leads
to ambiguous decision boundaries. Our key insight is that
segregating the label space into distinct base and novel subspaces inherently mitigates inter-domain bias.
To address this challenge, we propose a Hierarchical
Divide-and-Conquer Grouping (HDG) framework that hierarchically decomposes the label space into disentangled
subspaces, bypassing base-class dependency. For novel
classes lacking visual samples, we synthesize diversitypreserving yet discriminative training data by conditioning Stable Diffusion [28] on GPT-4-generated class descriptions [1], followed by semantic-level quality ﬁltering.
We partition the joint label space into base and novel subspaces by maximizing the Normalized Mutual Information
(NMI) [11] between novel-to-base and base-to-base similarity distributions. This enables fully automatic class separation, bypassing heuristic threshold selection. During inference, test samples are routed to subspace-speciﬁc classiﬁers, each comprising a frozen CLIP encoder augmented
with a trainable linear network.
Crucially, intra-subspace class confusion persists even
after initial partitioning, as shown in Fig. 1(b). HDG addresses this through recursive text-embedding clustering,
decomposing subspaces into focused subsets arranged hierarchically (Fig. 1(c)). This tree-structured approach enables
localized model specialization while preserving global coherence.
In summary, our main contributions are:
• We pioneer the Hierarchical Divide-and-Conquer
Grouping (HDG), the ﬁrst framework to eliminate baseclass dependency in zero-shot adaptation by hierarchically decomposing the label space into disentangled
subspaces. HDG reduces prediction bias by isolating
base/novel classes and resolving intra-domain confusion
through recursive K-means, establishing a new paradigm
for bias-free transfer learning.
• We propose a novel LLM-diffusion collaborative synthesis pipeline that synergizes GPT-4-generated seman-

Existing adaptation methods of pre-trained visionlanguage models like CLIP often rely on base-class samples during ﬁne-tuning, introducing systematic biases that
distort decision boundaries and degrade performance on
novel classes. In this work, we break new ground by proposing a hierarchical divide-and-conquer framework that addresses classiﬁcation bias at its root. Our method ﬁrst segregates the label space into base and novel subspaces, ensuring domain separation. Subsequently, it employs textembedding clustering within each subspace to decompose
ambiguous intra-domain classes into disentangled, ﬁnegrained clusters. This two-stage grouping strategy not only
alleviates class confusion but also enables domain-speciﬁc
model training in isolated subspaces, fostering specialized learning without overﬁtting base categories. Experiments on three classiﬁcation benchmarks reveal that our
approach achieves state-of-the-art performance, surpassing
the second-best competitor by 10% average accuracy.

1. Introduction
Recent advances in visual language models (VLMs), such
as CLIP [29] and ALIGN [17], have revolutionized crossmodal knowledge transfer. Current adaptation strategies
typically ﬁne-tune VLMs via task-speciﬁc learnable layers [14] or prompt engineering [2, 37], relying on limited
target-domain samples. These approaches assume identical training-test distributions but suffer severe performance
degradation under domain shifts—particularly in Generalized Zero-Shot Classiﬁcation (GZSC) scenarios [34]. When
test samples encompass both base (seen) and novel (unseen) classes, models ﬁne-tuned on base-class data exhibit
systematic prediction bias, disproportionately favoring base
classes due to overﬁtting [34].
We argue that the prediction bias arises inherently from
the transfer learning paradigm, which encounters test data
* Corresponding author.

3575

