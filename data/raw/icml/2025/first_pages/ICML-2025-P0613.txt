Adaptive Multi-prompt Contrastive Network
for Few-shot Out-of-distribution Detection
Xiang Fang 1 2 Arvind Easwaran 2 Blaise Genest 3
Abstract
as image classification (Bai et al., 2024; Gu et al., 2024;
Li et al., 2024a). Unfortunately, most of DNNs refer to a
Out-of-distribution (OOD) detection attempts to
closed-set assumption that all the test samples are seen durdistinguish outlier samples to prevent models
ing training and no outliers are observed during inference.
trained on the in-distribution (ID) dataset from
In fact, there are many unseen test samples (i.e., outliers) in
producing unavailable outputs. Most OOD dereal-world applications, such as autonomous driving (Zentection methods require many ID samples for
del et al., 2022; Vyas et al., 2018; Lu et al., 2023). These
training, which seriously limits their real-world
DNN methods still mistakenly classify each outlier into a
applications. To this end, we target a challengseen class. The wrong classification of outliers will result
ing setting: few-shot OOD detection, where only
in irrecoverable losses in some safety-critical scenarios. To
a few labeled ID samples are available. Theresolve the above problem, the out-of-distribution detection
fore, few-shot OOD detection is much more chal(OOD
detection) task (Gautam et al., 2023; Zhang et al.,
lenging than the traditional OOD detection set2024a;
Hendrycks & Gimpel, 2016; Sun & Li, 2022; Fort
ting. Previous few-shot OOD detection works
et
al.,
2021;
Liu et al., 2020) is proposed to accurately deignore the distinct diversity between different
tect
outliers
in
OOD classes and correctly classify samples
classes. In this paper, we propose a novel netfrom
in-distribution
(ID) classes during testing. Therefore,
work: Adaptive Multi-prompt Contrastive NetOOD
detection
has
attracted
increasing attention and variwork (AMCN), which adapts the ID-OOD sepaous
OOD
detection
models
have
been proposed for various
ration boundary by learning inter- and intra-class
safety-critical
scenarios
(Kirchheim
et al., 2024; Abrecht
distribution. To compensate for the absence
et
al.,
2024;
Kaur
et
al.,
2024;
Wu
et
al.,
2024). Most OOD
of OOD and scarcity of ID image samples, we
detection
works
(Shen
et
al.,
2024;
Regmi
et al., 2024a;
leverage CLIP, connecting text with images, enXue
et
al.,
2024;
Regmi
et
al.,
2024b;
Zhang
et al., 2024b)
gineering learnable ID and OOD textual prompts.
refer
to
a
fully-supervised
assumption
that
samples
of all
Specifically, we first generate adaptive prompts
types
(e.g.,
all
races
of
cats)
of
an
ID
class
(e.g.,
cat)
in all
(learnable ID prompts, label-fixed OOD prompts
situations
are
accessible
during
training,
which
is
unrealisand label-adaptive OOD prompts). Then, we gentic. Further, many OOD samples are also usually required
erate an adaptive class boundary for each class
during training.
by introducing a class-wise threshold. Finally,
we propose a prompt-guided ID-OOD separation
module to control the margin between ID and
OOD prompts. Experimental results show that
AMCN outperforms other state-of-the-art works.

Few-shot OOD detection is posed to first train the designed
model on a few samples in each ID class and then conduct
OOD detection on the whole test set. Such a setting limits the performance of standard OOD detection methods.
Existing few-shot OOD detection task meets the following
challenges: 1) Most few-shot methods (Jeong & Kim, 2020;
Dionelis et al., 2022; Mehta et al., 2024; Zhan et al., 2022;
Zhu et al., 2024) are sensitive to the background of the image. When they train the designed model on a few number of
images of each class, it might lead to the understanding bias,
resulting in the wrong OOD detection results. For example,
“dog” (ID) class and “wolf” (OOD) class share many visual
characteristics. When a dog appears on the grassland, the designed model might misidentify it as a wolf. Besides, some
similar classes have various backgrounds, which might lead
to wrong OOD detection reasoning results. For instance,
in a real-world training dataset, cat images are mainly indoors, while dog images are mostly outdoors. Thus, the

1. Introduction
Deep neural networks (DNNs) receive more and more attention due to their wide machine learning applications, such
1
Energy Research Institute @ NTU, Interdisciplinary Graduate Programme, Nanyang Technological University, Singapore
2
College of Computing and Data Science, Nanyang Technological
University, Singapore 3 CNRS and CNRS@CREATE, IPAL IRL
2955, France and Singapore. Correspondence to: Xiang Fang
<xiang003@e.ntu.edu.sg>.

Proceedings of the 42 nd International Conference on Machine
Learning, Vancouver, Canada. PMLR 267, 2025. Copyright 2025
by the author(s).

1

