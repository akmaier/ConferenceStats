Graph-based, Self-Supervised Program Repair from Diagnostic Feedback

Michihiro Yasunaga
LSTM

1

Percy Liang 1

LSTM

Abstract LSTM

Broken Program

Evaluator (compiler)

(`char` should be `string` instead in line 5)

LSTM of learning
LSTM to repair proWe consider the problem
Feedback
1 #include <bits/stdc++.h>
grams from diagnostic feedback (e.g., compiler
2 #include <string>
line 9:error: request for
3 using namespace std;
error messages). LSTM
Program repair
is challenging
LSTM
member ‘size’ in ‘a’,
4 int main() {
which is of non-class
for two reasons: First, it requires reasoning and
5
char tmp, a , b;
type ‘char’
6
map<string,int> mp;
tracking symbols across sourceLSTM
code and diag7
cin >> a >> b;
8
int i, j;
nostic feedback. Second, labeled datasets avail9
for (i = 0; i < a .size(); i++){
10
tmp.push_back( a [i]);
able for program repair are relatively
small. In
LSTM
11
string tmp1 = tmp;
this work, we propose novel solutions to these
12
for (j = 0; j < b.size(); j++){
13
tmp1.push_back(b[j]);
two challenges. First, we introduce a program14
mp[tmp1] = 1;
DrRepair (our model)
15
}
feedback graph, which connects symbols relevant
16
}
1. Error localized line 5
to program repair in source code and diagnostic
17
map<string,int>::iterator it;
18
it = mp.begin();
2. Repair
feedback, and then apply a graph neural network
Goal
19
cout << it.first << endl;
char tmp, a, b;
20
}
1.
Localize
error:
line
5
on top to model the reasoning process. Second,
→ string tmp, a, b;
Edit
we 2.present
a self-supervised learning paradigm
Example taken from SPoC dataset
char tmp, a, b;
( 909A-45398788.cpp )
for →
program
repair
thatb;leverages unlabeled prostring
tmp, a,
grams available online to create a large amount
Figure 1. Given a broken program and diagnostic feedback (comof extra program repair examples, which we use
piler error message), our goal is to localize an erroneous line and
generate a repaired line.
to pre-train our models. We evaluate our proposed approach on two applications: correcting
introductory programming assignments (DeepFix
errors has garnered significant interest recently (Gupta et al.,
dataset) and correcting the outputs of program
2017; Hajipour et al., 2019; Mesbah et al., 2019).
synthesis (SPoC dataset). Our final system, DrRepair, significantly outperforms prior work, achievIn this work, we consider the problem
of learning to reEvaluator (compiler)
with errors
ing 68.2% full repair rate on DeepFix (+22.9% Program
pair
programs
based
on
diagnostic
feedback
(compiler error
(`char` should be `string` instead in line 5)
over the prior best), and 48.4% synthesis success
messages). Figure 1 illustrates the setup. Given a broken
Feedback
1 #include <bits/stdc++.h>
rate on SPoC (+3.7% over the prior best).
program and diagnostic feedback, we aim to localize an
2 #include <string>
line 9:error: request
3 using namespace
erroneoustd;
line in the program and
generate a repaired line.
for member ‘size’ in
4 int main() {
‘
a’, which
is of
Learning
program
repair
has
two
major
challenges: First,
1. Introduction
5
char tmp, a , b;
non-class type ‘char’
6
map<string,int>
the
system mp;
needs to connect and jointly reason over the bro7
cin >> a >> b;
Automatic program repair has the potential to dramatically
8
intken
i, j;source code and the diagnostic feedback (Fitzgerald
improve the productivity of programming. In particular,
9
foret
(i =
< a size();
i++){
al.,0; i
2008).
Second,
existing works rely on manual effort
10
tmp.push_back( a [i]);
a common source of program errors are compiler errors,
to
curate
labeled
datasets
for program repair (e.g. hbroken
11
string tmp1 = tmp;
which include use of unresolved symbols, missing delimiters
12
for (j
= 0; j <
b.size();
j++){ pairs), which does not scale up
program,
fixed
programi
13
tmp1.push_back(b[j]);
(e.g. braces), and type errors. These errors are commonly
(Mesbah= et
14
mp[tmp1]
1; al., 2019). Here we present DrRepair, a novel
DrRepair (our model)
observed in both beginner programmers (Parihar et al., 2017)
15
}approach to program repair that addresses
these two chaland professional developers (Seo et al., 2014), as well as16in } lenges. Our key innovations 1.are
two-fold:
1) 5
modeling
Error
localized line
17
map<string,int>::iterator it;
the predicted code of program synthesis (Kulal et al., 2019).
18
it =ofmp.begin();
2. Repair
program repair with program-feedback
graphs and 2)
19
cout << it.first << endl;
char tmp, a, b;
Accordingly, the use of machine learning in fixing compiler
self-supervised learning with unlabeled
20 }
→ stringprograms.
tmp, a, b;
1
Stanford University, Stanford, CA. Correspondence to: Michihiro Yasunaga <myasu@cs.stanford.edu>.

Program-feedback
graph.
Example taken from SPoC
datasetProgram repair requires reasoning
( 909A-45398788.cpp)
jointly over the symbols (e.g. identifiers, types, operators)
across source code and diagnostic feedback. For instance,
in the example given in Fig. 1, while the compiler message
points to line 9, the error is related to the type of identi-

Proceedings of the 37 th International Conference on Machine
Learning, Online, PMLR 119, 2020. Copyright 2020 by the author(s).

Error type
Expected ...
● operator/punctuator
● primary expression

Statistics

Common compiler messages
expected @@@ (e.g. expected ‘;’ before...,
expected ‘}’ at end of input, expected
primary-expression before...)
missing @@@ (e.g. terminating " character)
redeclaration/conflicting declaration @@@

DeepDelta

DeepFix

SPoC

Avg.

9%

48%
● 37%
● 11%

35%
● 29%
● 6%

30%
● 23%
● 7%

Relevant auto-corruption module
(our proposal)
Syntax (deletion, insertion, replacement of
op/punc)
ID-typo (deletion, insertion of IDentifier)

Sourc

4 in
5 c
6 m
7 c
8 i
9 f
10
11
.

