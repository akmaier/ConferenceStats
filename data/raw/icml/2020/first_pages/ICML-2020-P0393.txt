Statistically Preconditioned Accelerated Gradient Method
for Distributed Optimization

Hadrien Hendrikx 1 Lin Xiao 2 Sébastien Bubeck 2 Francis Bach 1 Laurent Massoulié 1

Abstract

leave ψ mainly for non-smooth regularizations such as the
`1 -norm or the indicator function of a constraint set.

We consider the setting of distributed empirical
risk minimization where multiple machines compute the gradients in parallel and a centralized
server updates the model parameters. In order to
reduce the number of communications required
to reach a given accuracy, we propose a preconditioned accelerated gradient method where the
preconditioning is done by solving a local optimization problem over a subsampled dataset at
the server. The convergence rate of the method depends on the square root of the relative condition
number between the global and local loss functions. We estimate the relative condition number
for linear prediction models by studying uniform
concentration of the Hessians over a bounded domain, which allows us to derive improved convergence rates for existing preconditioned gradient methods and our accelerated method. Experiments on real-world datasets illustrate the benefits
of acceleration in the ill-conditioned regime.

In modern machine learning applications, the dataset is often
very large and has to be stored at multiple machines. For
simplicity of presentation, we assume N = mn, where m
is the number of machines and n is the number of samples
(j)
(j)
stored at each machine. Let Dj = {z1 , . . . , zn } denote
the dataset at machine j and define the local empirical risk
fj (x) =

n
1X
(j) 
` x, zi ,
n i=1

j = 1, . . . , m.

(3)

The overall empirical risk of Equation (2) can then be written as
m
m n
1 X
1 XX
(j) 
F (x) =
` x, zi .
fj (x) =
m j=1
nm j=1 i=1

We assume that F is LF -smooth and σF -strongly convex
over dom ψ, in other words,
σF Id  ∇2 F (x)  LF Id ,

∀x ∈ dom ψ,

(4)

where Id is the d × d identity matrix. The condition number
of F is defined as κF = LF /σF .

1. Introduction
We consider empirical risk minimization problems of the
form
minimize Φ(x) , F (x) + ψ(x),
(1)

We focus on a basic setting of distributed optimization where
the m machines (workers) compute the gradients in parallel
and a centralized server updates the variable x. Specifically,
during each iteration t = 0, 1, 2, . . .,

where F is the empirical risk over a dataset {z1 , . . . , zN }:

(i) the server broadcasts xt to all m machines;

x∈Rd

N

1 X
F (x) =
`(x, zi ),
N i=1

(2)

and ψ is a convex regularization function. We incorporate
smooth regularizations such as squared Euclidean norms
(λ/2)kxk2 into the individual loss functions `(x, zi ), and
1

INRIA, DIENS, PSL Research University, Paris, France.
Microsoft Research, Redmond, WA, USA.. Correspondence to:
Hadrien Hendrikx <hadrien.hendrikx@inria.fr>.
2

Proceedings of the 37 th International Conference on Machine
Learning, Online, PMLR 119, 2020. Copyright 2020 by the author(s).

(ii) each machine j computes the gradient ∇fj (xt ) and
sends it back to the server;
Pm
1
(iii) the server forms ∇F (xt ) = m
j=1 ∇fj (xt ) and
uses it to compute the next iterate xt+1 .
A standard way for solving problem (1) in this setting is to
implement the proximal gradient method at the server:
n
o
1
xt+1 = argmin ∇F (xt )>x+ψ(x)+
kx−xt k2 , (5)
2ηt
x∈Rd
where k · k denotes the Euclidean norm and ηt > 0 is the
step size. Setting ηt = 1/LF leads to linear convergence:
t L F
2
Φ(xt ) − Φ(x∗ ) ≤ 1 − κ−1
(6)
F
2 kx∗ − x0 k ,

